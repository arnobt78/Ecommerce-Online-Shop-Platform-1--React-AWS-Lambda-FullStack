# REQUIREMENTS.md — CodeBook E-Commerce Platform

**Cycle:** C1 (brownfield baseline) | **Status:** LIVE | **Last sync:** 2026-08-01
**Source of Truth** — all Build/Test/Verify agents trace to REQ-IDs here. Never edit code without a REQ-ID.

Legend — Status: `BASELINE` (pre-existing, shipped, not yet independently re-verified this cycle) · `IN-PROGRESS` · `PROPOSED` (not started) · `DONE` (verified this cycle). Risk tier per POLICY.yaml.

---

## 0001-0099 — Platform / Architecture

| REQ-ID | Requirement | Status | Risk | Evidence |
|---|---|---|---|---|
| REQ-0001 | Frontend is a React 19 SPA built with Create React App (react-scripts 5), served statically. | BASELINE | R1 | `package.json`, `src/index.js` |
| REQ-0002 | Client-side routing via React Router 7 with public storefront, auth, and protected admin route trees. | BASELINE | R1 | `src/routes/` |
| REQ-0003 | Server state managed via TanStack Query 5 (React Query) against the AWS API. | BASELINE | R1 | `src/services/`, `src/hooks/` |
| REQ-0004 | Backend is AWS Lambda (Node.js) behind an HTTP API Gateway; DynamoDB is the primary data store via AWS SDK v3. | BASELINE | R2 | `aws-lambda/functions/`, `aws-lambda/template.yaml` |
| REQ-0005 | Frontend deploys to Vercel; `vercel.json` configures routing/build. | BASELINE | R1 | `vercel.json`, `.vercel/` |
| REQ-0006 | Styling via Tailwind CSS 3 with a documented design system. | BASELINE | R0 | `tailwind.config.js`, `docs/UI_STYLING_GUIDE.md` |
| REQ-0007 | A top-level React error boundary catches render-time errors anywhere in the tree and shows a recoverable fallback UI instead of a blank white screen. | DONE (2026-07-30, build-verified) | R1 | Gap found during full-codebase audit — none existed previously. `src/components/Other/ErrorBoundary.js`, wired in `src/index.js` around `<App />`. |
| REQ-0008 | `FilterContext`'s price sort must not mutate `state.productList` in place. | DONE (2026-07-30) | R1 | Real bug found: `Array.prototype.sort()` mutates and returns the same reference; `bestSeller()`/`inStock()` return `state.productList` unchanged when their filters are off, so sorting it directly mutated reducer state outside of `dispatch` — a React anti-pattern that can cause stale/inconsistent renders. Fixed with `[...products].sort(...)` in `src/context/FilterContext.js`. |
| REQ-0009 | Remove `LoadingContext`/`LoadingProvider` — dead scaffolding with zero consumers (wrapped the whole app in `index.js` but `useLoading()` was never called anywhere). | DONE (2026-07-30, build-verified) | R0 | Deleted `src/context/LoadingContext.js`; removed the wrapper from `src/index.js`. |

## 0100-0199 — Authentication & Authorization

| REQ-ID | Requirement | Status | Risk | Evidence |
|---|---|---|---|---|
| REQ-0100 | User registration issues hashed credentials (bcryptjs) and a JWT on success. | BASELINE | R3 | `aws-lambda/functions/auth/register.js` |
| REQ-0101 | User login validates credentials and returns a signed JWT (jsonwebtoken). | BASELINE | R3 | `aws-lambda/functions/auth/login.js` |
| REQ-0102 | Role-based access control gates admin routes/pages from customer routes. | BASELINE | R3 | `src/context/`, `src/pages/Admin/` |
| REQ-0103 | Frontend Login/Register pages consume the auth API and persist session state. | BASELINE | R2 | `src/pages/Login.js`, `src/pages/Register.js` |
| REQ-0104 | The Login page's quick-login list must be sourced from the database (`User.isDemo`), not hardcoded frontend `REACT_APP_GUEST_LOGIN`/`REACT_APP_ADMIN_LOGIN`/`*_PASSWORD` env vars — no password should ever need to live in the client bundle for demo accounts. | DONE (2026-07-30, browser-verified) | R2 | User's direct question: "why do we have to add those in REACT_APP_GUEST_LOGIN... would that be better [in DB]?" — yes. Added `User.isDemo` (Prisma), `GET /auth/demo-accounts` (public, returns email/name/role only) and `POST /auth/demo-login` (issues a JWT for a vetted `isDemo` email, no password round trip) to `backend/`. Frontend: `useDemoAccounts()` hook + rewritten `Login.js` dropdown — selecting an account now logs in immediately (no more "fill form, then submit" two-step). Removed the 4 env vars from `.env.example`/`.env.local` entirely. Also fixed a latent bug found while touching this code: the old `handleLogin` always overwrote `sessionStorage.userRole` with the dropdown's `selectedRole` (defaulting to `"user"`) even for a normal password login — so a real admin typing their own credentials without touching the dropdown got demoted to `"user"` client-side. `persistSession()` (shared by login/register/demoLogin) already sets the correct role from the API response; the redundant, buggy overwrite was removed. |

## 0200-0299 — Product Catalog

| REQ-ID | Requirement | Status | Risk | Evidence |
|---|---|---|---|---|
| REQ-0200 | List/browse products with filtering. | BASELINE | R1 | `aws-lambda/functions/products/list.js`, `src/pages/Products/` |
| REQ-0201 | Product detail view by ID. | BASELINE | R1 | `aws-lambda/functions/products/get.js`, `src/pages/ProductDetail.js` |
| REQ-0202 | Admin create/update/delete product CRUD. | BASELINE | R2 | `aws-lambda/functions/products/{create,update,delete}.js`, `src/pages/Admin/AdminProduct*Page.js` |
| REQ-0203 | Featured products surfaced on the storefront home page. | BASELINE | R1 | `src/pages/Home/components/FeaturedProducts.js` |
| REQ-0204 | Extend the product schema with `author`, `category`, `level`, `pages` — enriched, real-world catalog metadata beyond the original AWS/DynamoDB shape — and replace all 15 seed products' Lorem-ipsum `overview`/`long_description` with distinct, professionally written copy per book. | DONE (2026-07-30, browser-verified) | R1 | User's direct request: "rewrite the seed data of data/db.json... with proper all extended enriched schema with all meaningful real-world professional... data." Added the 4 fields to the Prisma `Product` model + backend create/update service + `ProductForm.js` (author text input, category/level `FormSelect` dropdowns, pages number input) + display on `ProductDetail.js`, `ProductCard.js`, `AdminProductDetailPage.js`. Rewrote all 15 products in `data/db.json` (and mirrored the 3 `featured_products` entries + the 2 `orders[0].cartList` items) with unique, topic-specific copy — no more shared boilerplate paragraph. `backend/prisma/seed.js`'s product upsert changed from `update: {}` (no-op on re-run) to a real `update:` mirroring `create:`, so re-seeding actually syncs `data/db.json` content instead of silently keeping stale rows. Verified live: `GET /products` returns the new fields; `ProductDetail.js` renders author/category/level/pages badges. |

## 0300-0399 — Cart & Checkout / Payments

| REQ-ID | Requirement | Status | Risk | Evidence |
|---|---|---|---|---|
| REQ-0300 | Client-side cart (add/remove/update quantity) via reducer/context. | BASELINE | R1 | `src/reducers/`, `src/pages/Cart/` |
| REQ-0306 | Cart survives a page refresh/reopen (persisted to `localStorage`, keyed `codebook-cart`), not just in-memory `useReducer` state. | DONE (2026-07-30, browser-verified) | R1 | Gap found during audit: cart was pure in-memory, refreshing the page silently emptied it — a real e-commerce bug. Fixed in `src/context/CartContext.js` via lazy `useReducer` init + a persistence `useEffect`; still clears on user switch (existing behavior, unchanged). Verified live: added item, hard-reloaded, cart badge still showed the item. |
| REQ-0301 | Stripe Checkout / Payment Element integration for order payment. | BASELINE | R3 | `src/pages/Cart/components/StripeCheckout.js`, `@stripe/react-stripe-js` |
| REQ-0302 | Backend creates a Stripe PaymentIntent server-side. | BASELINE | R3 | `aws-lambda/functions/payment/create-intent.js` |
| REQ-0303 | Backend verifies payment status post-checkout. | BASELINE | R3 | `aws-lambda/functions/payment/verify.js` |
| REQ-0304 | Stripe webhook handler reconciles payment events with orders. | BASELINE | R3 | `aws-lambda/functions/payment/webhook.js` |
| REQ-0305 | Payment success/cancel pages reflect final payment outcome to the user. | BASELINE | R2 | `src/pages/Payment/PaymentSuccessPage.js`, `src/pages/Payment/PaymentCancelPage.js` |

## 0400-0499 — Orders & Fulfillment

| REQ-ID | Requirement | Status | Risk | Evidence |
|---|---|---|---|---|
| REQ-0400 | Customer order creation/retrieval. | BASELINE | R2 | `aws-lambda/functions/orders/index.js`, `src/pages/Order/OrderPage.js` |
| REQ-0401 | Order success/fail confirmation views. | BASELINE | R1 | `src/pages/Order/components/OrderSuccess.js`, `OrderFail.js` |
| REQ-0402 | Admin order list + detail views. | BASELINE | R2 | `aws-lambda/functions/admin/{orders,order-detail}.js`, `src/pages/Admin/AdminOrders*Page.js` |
| REQ-0403 | Admin order status transitions. | BASELINE | R2 | `aws-lambda/functions/admin/order-status.js` |
| REQ-0404 | Admin adds shipping tracking to an order (Shippo). | BASELINE | R2 | `aws-lambda/functions/admin/add-tracking.js` |
| REQ-0405 | Admin generates shipping labels (Shippo). | BASELINE | R2 | `aws-lambda/functions/admin/generate-label.js` |
| REQ-0406 | Admin issues refunds against an order (Stripe). | BASELINE | R3 | `aws-lambda/functions/admin/refund-order.js` |
| REQ-0407 | Admin order history view. | BASELINE | R1 | `src/pages/Admin/AdminHistoryPage.js` |

## 0500-0599 — Reviews

| REQ-ID | Requirement | Status | Risk | Evidence |
|---|---|---|---|---|
| REQ-0500 | Customers create/list/update/delete product reviews. | BASELINE | R2 | `aws-lambda/functions/reviews/*.js` |
| REQ-0501 | Admin moderates reviews (list, approve/update, status). | BASELINE | R2 | `aws-lambda/functions/admin/{reviews,review-update}.js`, `src/pages/Admin/AdminReviewsPage.js` |

## 0600-0699 — Support Tickets

| REQ-ID | Requirement | Status | Risk | Evidence |
|---|---|---|---|---|
| REQ-0600 | Customer creates a support ticket. | BASELINE | R1 | `aws-lambda/functions/tickets/create.js`, `src/pages/Tickets/CreateTicketPage.js` |
| REQ-0601 | Customer/admin list and view ticket detail + thread. | BASELINE | R1 | `aws-lambda/functions/tickets/{list,get}.js`, `src/pages/Tickets/` |
| REQ-0602 | Reply to a ticket (customer or admin). | BASELINE | R1 | `aws-lambda/functions/tickets/reply.js` |
| REQ-0603 | Admin updates ticket status. | BASELINE | R1 | `aws-lambda/functions/tickets/update-status.js`, `src/pages/Admin/AdminTicketsPage.js` |

## 0700-0799 — Notifications

| REQ-ID | Requirement | Status | Risk | Evidence |
|---|---|---|---|---|
| REQ-0700 | Unread notification count for the current user. | BASELINE | R1 | `aws-lambda/functions/notifications/count.js` |
| REQ-0701 | Mark notification(s) as read. | BASELINE | R1 | `aws-lambda/functions/notifications/mark-read.js` |

## 0800-0899 — Admin Dashboard & Analytics

| REQ-ID | Requirement | Status | Risk | Evidence |
|---|---|---|---|---|
| REQ-0800 | Admin dashboard landing with key metrics. | BASELINE | R1 | `src/pages/Admin/AdminDashboardPage.js` |
| REQ-0801 | Admin analytics page (charts via Recharts). | BASELINE | R1 | `src/pages/Admin/AdminAnalyticsPage.js` |
| REQ-0802 | Admin user management (list/detail/edit/delete). | BASELINE | R3 | `aws-lambda/functions/admin/user-*.js`, `src/pages/Admin/AdminUser*Page.js` |
| REQ-0803 | Admin settings page. | BASELINE | R2 | `src/pages/Admin/AdminSettingsPage.js` |
| REQ-0804 | Customer-facing dashboard (order/account summary). | BASELINE | R1 | `src/pages/Dashboard/DashboardPage.js` |

## 0900-0999 — Email / Transactional Messaging

| REQ-ID | Requirement | Status | Risk | Evidence |
|---|---|---|---|---|
| REQ-0900 | Transactional email sending via Brevo integration. | BASELINE | R2 | `aws-lambda/functions/email/send-email.js` |

## 1000-1099 — Activity Logging / Audit Trail

| REQ-ID | Requirement | Status | Risk | Evidence |
|---|---|---|---|---|
| REQ-1000 | Admin activity log capturing admin actions for audit. | BASELINE | R2 | `aws-lambda/functions/admin/activity-logs.js` |

## 1100-1199 — Repo Restructure / Infra Migration (OPEN THREAD)

| REQ-ID | Requirement | Status | Risk | Evidence |
|---|---|---|---|---|
| REQ-1101 | Flatten repo layout: move former `codebook/` subfolder contents (src, aws-lambda, public, scripts, lib, docs, config files) up to repo root as the single project root. | **DONE (2026-08-01, committed `14e53b2`)** | R2 | Repo root is the live project; old `codebook/` path gone from tree |
| REQ-1102 | Remove archived reference-only backend implementations (`backend-mock-json-server-project-reference/`, `backend-vercel-serverless-functions-project-reference/`) no longer used by the live AWS Lambda backend. | **DONE (2026-08-01, committed `14e53b2`)** | R1 | Archived reference folder removed; live backend is `backend/` (Express) |
| REQ-1103 | `.gitignore` updated to match the new flattened root layout. | **DONE (2026-08-01, committed `14e53b2`)** | R0 | Root `.gitignore` tracks Vite/Express layout |

> CKPT-0001 closed: restructure left uncommitted until user authorized commit; shipped in `14e53b2` (2026-08-01) with REQ-1200…1630.

## 1200-1299 — Backend Platform Migration (AWS Lambda+DynamoDB → Express+Postgres on VPS)

**Trigger:** AWS free-tier account (849695476938) closes 2026-08-16; current production backend (AWS Lambda + API Gateway + DynamoDB) will become unreachable and all DynamoDB data will be permanently deleted after that date. See `RISK_REGISTER.md` RISK-0005 (CRITICAL).

| REQ-ID | Requirement | Status | Risk | Notes |
|---|---|---|---|---|
| REQ-1200 | Stand up an Express.js (Node 22) backend that mirrors all 33 existing Lambda endpoints 1:1 — same paths, methods, request/response shapes. No functional/behavioral changes vs. current API. | DONE (2026-07-30, local build + smoke-tested) | R3 | Route inventory: `aws-lambda/template.yaml` (33 functions across products, auth, orders, admin, payment, email, notifications, tickets, reviews) |
| REQ-1201 | Provision a PostgreSQL database (`codebook_db` / `codebook_user`) on the user's existing shared Coolify Postgres container (`xok0c8w8808g8080og4gccwc`, `postgres:17-alpine`, Hetzner VPS `77.42.71.87`), following the established one-container-many-databases convention already used for 8+ other projects. | PROPOSED | R2 | Per `docs/HETZNER_VPS_MIGRATION_GUIDE.md` |
| REQ-1202 | Design a relational schema (Prisma) for 6 domains mapped from current DynamoDB tables: `codebook-products`, `codebook-orders`, `codebook-users`, `codebook-activity-log`, `codebook-tickets`, `codebook-reviews`. Notifications remain a computed query (no dedicated table today). | DONE (2026-07-30, local build + smoke-tested) | R2 | Prisma chosen for consistency with user's existing VPS convention (`prisma db push`, no shadow-DB migrations) |
| REQ-1203 | ~~Export live DynamoDB data to JSON before AWS closes.~~ | **WONT-DO (2026-07-30)** | — | User decision: production DynamoDB data is not needed. Superseded by REQ-1204. |
| REQ-1204 | Idempotent Prisma seed script loading: (a) product catalog from `data/db.json` (15 products, `featured_product` flag preserved, no separate table); (b) two fresh test accounts — `test@admin.com` (role `admin`) and `test@user.com` (role `user`), both password `12345678`; (c) empty `tickets`/`reviews`/`activity_log` tables (no legacy source data — these are AWS-era-only domains not present in `data/db.json`). Replaces live-data migration per user decision. | DONE (2026-07-30, local build + smoke-tested) | R1 | Depends on REQ-1202. `data/routes.json` is legacy json-server routing config, not seed data — reference only. Emails corrected 2026-07-30 to match `docs/DROPDOWN_TEST_CREDENTIALS_DOCS.md`'s own reference pattern (`test@user.com`/`test@admin.com`), which is what the user was actually recalling. |
| REQ-1205 | Port `aws-lambda/shared/*.js` (auth, response, products, orders, users, reviews, tickets, activityLog, qrcode) to Express middleware/services with equivalent function contracts — same business logic, new transport layer. | DONE (2026-07-30, local build + smoke-tested) | R2 | |
| REQ-1206 | Preserve JWT bearer-token auth exactly as-is (`jsonwebtoken` + `bcryptjs`, 7-day expiry, role embedded in token) in this phase — no change to frontend token storage (`sessionStorage`) yet. | DONE (2026-07-30, local build + smoke-tested) | R3 | httpOnly-cookie hardening deferred as a future, separately-scoped improvement, not required for parity migration |
| REQ-1207 | Preserve Stripe integration (create-intent, webhook, verify) — webhook signature verification re-pointed at the new backend's public URL. | DONE (2026-07-30, local build + smoke-tested) | R3 | Stripe dashboard webhook endpoint URL must be updated at cutover |
| REQ-1208 | Preserve Cloudinary, Brevo, and Shippo integrations unchanged — external services, config-only dependency, no code logic changes required. | DONE (2026-07-30, local build + smoke-tested) | R1 | |
| REQ-1209 | Restrict CORS to actual frontend origin(s) via `CORS_ORIGINS` env (replacing the current Lambda `AllowOrigins: "*"`), per the project's own Coolify convention. | DONE (2026-07-30, local build + smoke-tested) | R2 | |
| REQ-1210 | Deploy the new backend via Dockerfile + Coolify on the Hetzner VPS at a new subdomain (e.g. `codebook-backend.arnobmahmud.com`) + sslip.io fallback, following `docs/SUBDOMAIN_ARNOBMAHMUD_SETUP.md` and `docs/COOLIFY_PUBLIC_BACKEND_GUIDE.md`. | PROPOSED | R2 | |
| REQ-1211 | Update frontend API base URL (env var, currently `REACT_APP_LAMBDA_API_URL` in `src/services/apiClient.js`) to the new backend URL; remove the AWS API Gateway URL. | PROPOSED | R2 | |
| REQ-1212 | Decommission the AWS Lambda/API Gateway/DynamoDB stack only after production cutover is verified via parallel-run — never delete-first. | PROPOSED | R1 | Moot after 2026-08-16 regardless (account auto-closes) |
| REQ-1213 | Audit root-level `lib/` (duplicate of `aws-lambda/shared/`, uses `@aws-sdk` directly) — determine if it is dead code left over from the archived Vercel-serverless-functions backend (REQ-1102); remove only if confirmed unused. | DONE (2026-07-30) | R1 | Confirmed zero references outside the DynamoDB one-off `scripts/*.js` admin/migration scripts, which are themselves obsolete under the Postgres migration. Removed `lib/`, those 8 scripts, `scripts/dev.js`+`find-port.js` (wrapped `vercel dev`, which has nothing left to serve now that the archived `api/` folder is gone), and the dead `@aws-sdk/*`/`jsonwebtoken`/`bcryptjs`/`uuid` deps + `vercel:dev` script from root `package.json`. Trimmed the now-dead `/api/*` rewrite+headers from `vercel.json` (frontend stays on Vercel per `PROJECT_PLAN.md` §3, only the `/api` remnant was removed). |
| REQ-1214 | Full-codebase audit for ESLint errors/warnings, dead code, and leftover debug logging, per direct user request (implementation-standards prompt, 2026-07-30). | DONE (2026-07-30, build-verified) | R1 | Root cause found: `package.json`'s `eslintConfig.rules` globally disabled `no-unused-vars`, masking real warnings — removed the override and fixed all 20 findings it uncovered (dead imports, dead local vars/functions) across 17 files. Also removed dead DynamoDB-era code paths never reachable from the new Postgres backend: `useMigrateFeaturedProducts`/`useMigrateFeaturedToNumber` hooks + their service functions (`migrateFeaturedProducts`, `migrateFeaturedToNumber`, `getFeaturedProductsCount`) — all unused, called AWS-only migration endpoints; `getFeaturedList` (superseded by client-side filtering in `useFeaturedProducts`). Stripped all emoji-prefixed `console.log` debug traces (dev scaffolding, not production logging) from `useReviews.js`, `useAdmin.js`, `PaymentSuccessPage.js`, `ProductForm.js`, `StripeCheckout.js`, `adminService.js`, `reviewService.js` — kept legitimate `console.error` on real failure paths (e.g. background email send failures) and backend's operational logs (server start, webhook event trace). Also fixed a duplicated-branch / unreachable-`return` bug found in `adminService.js`'s `updateProduct` (dead code left behind by an earlier debug-log edit) and a real copy-paste bug where `deleteProduct` logged a stale "Product updated successfully" debug message. Frontend production build: 0 errors/warnings both before and after (warnings were suppressed, not absent) — now genuinely 0 with the suppression removed. |

## 1300-1399 — TypeScript Migration (frontend + backend)

| REQ-ID | Requirement | Status | Risk | Notes |
|---|---|---|---|---|
| REQ-1300 | Convert all frontend `.js`/`.jsx` under `src/` to strict-mode `.ts`/`.tsx`, preserving current UI/behavior exactly — no visual or functional changes. | DONE (2026-07-31) | R2 | 0 `.js`/`.jsx` remain anywhere under `src/`; `tsc -b` + `eslint` clean; verified in a real browser across every page type |
| REQ-1301 | Add TypeScript (strict) to the new Express backend; convert all backend source to `.ts`. | DONE (2026-07-30) | R2 | |
| REQ-1302 | Single source of truth for API request/response DTOs — Prisma-generated types on the backend; shared/generated types (not hand-duplicated) on the frontend. | DONE (2026-07-30) | R2 | |
| REQ-1303 | Migrate build tooling from Create React App (react-scripts) to Vite as part of the TypeScript pass — same React 19 / React Router / component code, new bundler. Matches `PROJECT_ENGINEERING_PLAYBOOK.md` "Vite SPA" mode. | DONE (2026-07-30) | R2 | User chose "Migrate CRA → Vite" via Human-Decision. |
| REQ-1304 | Introduce Zod (or equivalent) input validation at all backend API boundaries as part of the TS conversion. | DONE (2026-07-30) | R2 | |
| REQ-1305 | Eliminate `any`; strict null checks; discriminated unions for API success/error results. | DONE (2026-07-31) | R1 | True across the entire codebase, backend and frontend |
| REQ-1306 | Restructure static assets: move bundler-imported `src/assets/*` into `public/`, flatten the redundant `public/assets/images/` wrapper to `public/images/`. | DONE (2026-07-31) | R1 | Added mid-Phase-2 at user request; `data/db.json` + DB reseeded to match |
| REQ-1307 | Self-host fonts instead of Google Fonts CDN — drop unused imports, serve the actually-used font(s) from `public/`. | DONE (2026-07-31) | R1 | Added mid-Phase-2 at user request; Poppins removed (unused), Roboto self-hosted, bootstrap-icons moved to npm |
| REQ-1308 | Reconcile git tracking — the entire live codebase (`src/`, `backend/src/`, `data/`, `docs/`, config) was untracked while the git index still pointed at a stale pre-restructure `codebook/` subfolder path (258 files, physically gone from disk). | DONE (2026-07-31) | R2 | Deployment-blocking: any git-based CI/deploy would have built from stale/missing paths. Reconciled via `git add -A` (staged only, not committed — user has not authorized a commit this round). |
| REQ-1309 | Deep audit pass: fix all remaining ESLint errors/warnings, remove dead/unused code and debug artifacts, close TanStack Query performance gaps, per explicit user request to get the codebase deployment-ready. | DONE (2026-07-31) | R1 | See STATE.md eighth-pass write-up for the itemized list |
| REQ-1310 | Persist the TanStack Query cache to localStorage (stale-while-revalidate) so a reload/reopened tab paints from last-known-good data instantly instead of blank-loading, without leaking data across accounts. | DONE (2026-07-31) | R1 | `@tanstack/react-query-persist-client` + `query-sync-storage-persister`; `buster` tied to `package.json` version; relies on existing `queryClient.clear()` at every logout call site to prevent cross-account leakage — verified live (8 queries → 1 immediately after logout) |
| REQ-1311 | Code-split the admin console (`/admin/*`) into a separate lazy-loaded chunk so regular customers never download admin-only JS (charts, tables, forms) in their initial bundle. | DONE (2026-07-31) | R1 | `React.lazy` + `Suspense` in `AllRoutes.tsx`, importing from `pages/Admin` directly (not the mixed root `pages` barrel) so the chunk boundary actually holds; ~139KB/28KB-gzip extracted from the main bundle; idle-time prefetch added in `App.tsx` for already-logged-in admins so their first `/admin` click still feels instant |
| REQ-1612 | Auto-generate + attach + email an invoice PDF on successful order. | DONE (2026-07-31) | R1 | `pdfkit` (pure-JS, no headless-browser dep) generates the PDF server-side in `backend/src/services/invoice.service.ts`; attached to the existing `order-confirmation` Brevo email via a new optional `attachment` param on `sendEmailViaBrevo`. Invoice number derived deterministically from the order id (no schema migration / counter needed). PDF generation failure never blocks the confirmation email itself (try/catch, logs and falls back to sending without the attachment). Verified in isolation: generated PDF is valid (`file` confirms `PDF document, version 1.3`), correct line items/totals, and a layout bug (full-UUID order # wrapping into the Date line) was caught visually and fixed with dynamic y-positioning before considering this done. |
| REQ-1611 | TanStack Table migration with dropdown row actions across every admin list page. | DONE (2026-07-31) | R1 | Built two new reusable `src/components/ui/` primitives — `DataTable` (generic `@tanstack/react-table` wrapper, visual output matches the old `SortableTable` 1:1 so no page needed a redesign) and `DropdownMenu` (hand-built shadcn-style, same pattern as `AlertDialog`) — then migrated all 6 admin list pages (Products, Orders, Users, Reviews, Tickets, History) from the hand-rolled `SortableTable` + inline action buttons to `DataTable` + `DropdownMenuItem` row actions. Added an `onRowClick` prop to `DataTable` to preserve `AdminTicketsPage`'s whole-row-clickable behavior. Deleted the now-dead `SortableTable.tsx`. Verified live in the browser: sorting, dropdown open/close, click-outside-to-close, and row-click navigation all work correctly with 0 console errors on all 6 pages. |
| REQ-1605 | Full Bootstrap-Icons (`bi-*`) → `lucide-react` icon sweep across the entire codebase, for one consistent icon system. | DONE (2026-07-31) | R1 | Replaced all ~90 `bi-*` icon usages across ~40 files (shared layout components, all `components/ui/` primitives, every admin page, and every customer-facing page/component) with the equivalent `lucide-react` component, matching the sizing/`strokeWidth` convention already established by the admin KPI cards (`AdminMetricsCard.tsx`). Fixed one real latent bug surfaced during the sweep: `DropdownLoggedIn.tsx`'s click-outside handler selected the logged-out user icon by its `.bi-person-circle` CSS class, which broke once that icon became a Lucide SVG — fixed by giving both the logged-in and logged-out account-menu buttons the same `data-user-avatar-trigger` attribute and simplifying the selector to use only that. Removed the now-fully-unused `bootstrap-icons` npm dependency and its `index.css` `@import` — shrank the production CSS bundle from 141KB to 61KB. Verified `grep -rn "bi-[a-z]" src` returns zero matches; full real-browser pass across products list, product detail, dashboard, cart, and all 6 admin list pages confirmed correct rendering (filled/outline stars, stock badges, dropdown menus, sort arrows) with 0 console errors. |

## 1400-1499 — Cache / Invalidation / Realtime UX Contract

| REQ-ID | Requirement | Status | Risk | Notes |
|---|---|---|---|---|
| REQ-1400 | Establish a single shared `queryKeys` module for all TanStack Query keys, replacing any ad-hoc string keys scattered across `hooks/*.js`. | PROPOSED | R1 | |
| REQ-1401 | Every CRUD mutation across the app must invalidate/precisely-patch all affected query keys (current page + related list/detail/dashboard/badge views) with no full page reload — audit existing hooks against this contract and close gaps. | DONE (2026-07-30, browser-verified) | R2 | Audited every `useMutation` call site in `src/` (`useAdmin.js`, `useReviews.js`, `useTickets.js`, `useNotifications.js`, `usePayment.js`, `PaymentSuccessPage.js`'s inline order-create mutation — `useProducts.js`/`useUser.js` are read-only, no mutations). Fixed the one real gap: `useCreateReview`/`useUpdateReview` weren't invalidating `admin-reviews`. Live-verified end-to-end in a real browser against local Postgres: place order → cart badge cleared instantly, order appeared on customer dashboard with no refresh, order appeared in admin orders list, admin status change (Pending→Processing) updated the table cell instantly, admin dashboard revenue/order-count updated on next visit, product stock decremented (15→14) and showed correctly in admin products, review submission (with real order-ownership check) rendered on the product page immediately. |
| REQ-1409 | Full local browser-driven integration test of the migrated stack (frontend `react-scripts start` on :3000 against `backend/` on :4000, real local Postgres, real Stripe test-mode keys) before any deployment. | DONE (2026-07-30, browser-verified via Playwright) | R2 | User's explicit request: complete the migration and verify entirely on localhost before touching Coolify/VPS. Covered: homepage/featured products, login via test-account dropdown (both roles), RBAC route protection (customer blocked from `/admin/orders`), full Stripe test-card checkout (`4242...`) → real order created, cache invalidation (see REQ-1401), stock decrement, review creation, `UserAvatar`/Robohash rendering. `backend/.env` (local Postgres + real Stripe/Brevo/Shippo test keys reused from the already-gitignored `aws-lambda/.env.secrets`, Google OAuth left blank — untested this pass) and root `.env.local` (gitignored, overrides `REACT_APP_LAMBDA_API_URL` + test-account emails to point at the local backend) created for this — the committed `.env` is untouched and still points at AWS. |
| REQ-1402 | Realtime/cross-tab sync scope: TanStack Query invalidation only — audit and fix mutation hooks so every CRUD action refreshes all affected views within the current session instantly. No SSE/WebSocket. | DECIDED (2026-07-30) | R1 | User chose "TanStack Query invalidation only" via Human-Decision. Revisit only if a real cross-tab/cross-user need emerges (YAGNI). |
| REQ-1403 | Evaluate Redis need per YAGNI (`PROJECT_ENGINEERING_PLAYBOOK.md` §3.4) — do not introduce until a measured need exists; single small VPS instance has no current multi-instance cache-sharing requirement. | PROPOSED | R0 | Default: skip Redis for v1 of the migration |

## 1500-1599 — Google OAuth Sign-In (new feature, not present in the original AWS backend)

**Trigger:** user request, 2026-07-30 — "Continue with Google" one-click sign-in on both Login and Register pages. Credentials (Client ID + Client Secret) provided in chat.

| REQ-ID | Requirement | Status | Risk | Notes |
|---|---|---|---|---|
| REQ-1500 | Add a "Continue with Google" button to the Login page, below the existing "Log In" button. | DONE (2026-07-30, local build + smoke-tested) | R2 | `src/pages/Login.js` |
| REQ-1501 | Add a "Continue with Google" button to the Register page, below the existing "Sign Up" button. | DONE (2026-07-30, local build + smoke-tested) | R2 | `src/pages/Register.js` |
| REQ-1502 | Backend Google OAuth2 authorization-code flow: `GET /auth/google` (redirect to Google) and `GET /auth/google/callback` (exchange code, find-or-create user, issue the app's own JWT) — consistent with the redirect-callback pattern already used across the user's other Coolify-hosted projects (`docs/SUBDOMAIN_ARNOBMAHMUD_SETUP.md` §5, e.g. `/api/auth/callback/google`). | DONE (2026-07-30, local build + smoke-tested) | R3 | New Express backend only — no AWS Lambda equivalent existed |
| REQ-1503 | Frontend callback route (e.g. `/auth/callback`) that receives the issued JWT, then runs the same session bootstrap as normal email/password login (store token+role in `sessionStorage`, clear/prefetch React Query cache, navigate to `/products`). | DONE (2026-07-30, local build + smoke-tested) | R2 | Reuses `Login.js`'s existing post-login logic — no duplicated session-init code |
| REQ-1504 | Google OAuth credentials (Client ID + Client Secret) live only in gitignored `.env`/`.env.local` locally and in Coolify environment variables in production. **Never** committed to git, **never** placed in any tracked doc or source file — including this plan. | DONE (2026-07-30, local build + smoke-tested) | R3 | See `RISK_REGISTER.md` RISK-0007 |
| REQ-1505 | New Google-authenticated users default to role `user` (customer), same as normal registration. Exact behavior for an email that already has a password-based account (link vs. reject vs. separate account) is an implementation decision for the Constrain phase, not decided here. | DONE (2026-07-30, local build + smoke-tested) | R2 | Flagged, not blocking — low ambiguity risk, common pattern (link by verified email) |
| REQ-1506 | Expand the `users` schema (Prisma `User` model) with fields not present in the current DynamoDB `codebook-users` table: `image`/`avatar_url` (nullable string — profile picture URL), `google_id` (nullable, unique — links a Google account), `email_verified` (nullable timestamp). `password` becomes nullable (Google-only accounts have none). `role` already exists today (`aws-lambda/shared/users.js` defaults new users to `role: "user"`) — not new, just carried over. | DONE (2026-07-30, local build + smoke-tested) | R2 | See `PROJECT_PLAN.md` §4 for the full schema table |
| REQ-1507 | Reusable `UserAvatar` component (frontend): if the user has a stored `image` (from Google, or later a manual upload), display it; otherwise fall back to a deterministic generated avatar via Robohash (`https://robohash.org/<user id>?set=set4`) so every user always has a visually distinct avatar. Used wherever a user's avatar appears (navbar, admin user list/detail, ticket threads, reviews) — one component, not ad-hoc `<img>` tags per page. | DONE (2026-07-30, local build + smoke-tested) | R1 | Mirrors the existing `SafeImage`-style "try real thing, fall back gracefully" pattern already documented in `docs/SAFE_IMAGE_REUSABLE_COMPONENT.md`, adapted for avatars instead of `next/image` |
| REQ-1508 | The OAuth `state` param generated in `GET /auth/google` must actually be verified on `GET /auth/google/callback`, not just round-tripped through the URL — otherwise the CSRF protection the code comments claimed was never real. | DONE (2026-07-30, local smoke-tested) | R3 | Gap found during audit: `state` was generated and sent to Google but the callback never checked it. Fixed via a short-lived (5 min) httpOnly `SameSite=Lax` cookie set on `/auth/google`, compared against `req.query.state` on callback (mismatch/missing → redirect with `error=invalid_state`); cookie cleared either way. No new dependency added (manual cookie parse, matching the existing "dependency-free" convention in `googleOAuth.js`). Verified live: missing state, mismatched state, and missing code all redirect with the correct error; matching state proceeds to token exchange. |
| REQ-1509 | Wire the real Google Client ID/Secret (given by the user in chat earlier) into `backend/.env` for local testing, and confirm the `/auth/google` redirect actually hits Google's real consent screen (not a placeholder). | DONE (2026-07-30, redirect verified) | R3 | Confirmed live: `curl -I http://localhost:4000/auth/google` redirects to `accounts.google.com` with the real `client_id`. **Not** verified end-to-end: completing the actual Google consent screen requires a real interactive human login (email/password, possibly 2FA) — an automated browser agent cannot and should not attempt this (Google actively blocks/flags automated login attempts). The callback exchange (`exchangeCodeForTokens`/`getGoogleUserInfo`/`findOrCreateGoogleUser`) is implemented and was unit-verified against fake codes in the CSRF test (REQ-1508); a real round trip needs the user to click the button themselves once, and `GOOGLE_CALLBACK_URL` (`http://localhost:4000/auth/google/callback`) needs to actually be registered as an Authorized redirect URI in Google Cloud Console or Google will reject it with `redirect_uri_mismatch`. |

## 1600-1699 — UI/UX Consistency (toasts, confirmations, content quality)

**Trigger:** user request, 2026-07-30 — replace react-toastify with shadcn/ui's Sonner (dynamic title/subtitle per CRUD operation), add AlertDialog confirmation to destructive admin actions, and eliminate remaining placeholder/dummy copy across the storefront.

| REQ-ID | Requirement | Status | Risk | Notes |
|---|---|---|---|---|
| REQ-1600 | Replace `react-toastify` with shadcn/ui's Sonner across the entire codebase (33 files). Every toast gets a short, CRUD-operation-derived title (e.g. "Created", "Updated", "Deleted", "Sign In Failed") with the original message as the subtitle — not one flat line of text. | DONE (2026-07-30, browser-verified) | R1 | New `src/lib/toast.js` wraps Sonner behind the same `toast.success(message, options)` call signature react-toastify used, so all 33 call sites only needed an import-path swap (`closeButton`/`position` options are now accepted-but-ignored — every call already passed the same `{ closeButton: true, position: "bottom-right" }`, now set once globally on `<Toaster>` in `index.js`). Title derivation is regex-based on the message text (e.g. "credential"/"sign in" → "Sign In Failed"), not hand-written per call site — genuinely dynamic, not hardcoded, and scales to new call sites automatically. `<Toaster richColors closeButton position="bottom-right" />` gives success=green/error=red/warning=amber/info=blue automatically. Removed `react-toastify` from `package.json` entirely (CSS bundle shrank ~2.5KB). Verified live: failed login shows "Sign In Failed" / "Invalid credentials". |
| REQ-1601 | Surface the backend's actual error message (e.g. "Invalid credentials") instead of the generic HTTP status text (e.g. "Unauthorized") in toasts. | DONE (2026-07-30, browser-verified) | R1 | Gap found while testing REQ-1600: `authService.js`'s `login`/`register`/`demoLogin`/`getDemoAccounts` all did `throw new ApiError(response.statusText, response.status)`, discarding the JSON error body every other service (`reviewService.js`, `adminService.js`, etc.) already parsed correctly. Added a shared `throwApiError()` helper and fixed all 4 call sites. |
| REQ-1602 | Use the existing shadcn-style `AlertDialog` component (`src/components/ui/alert-dialog.js`) to confirm destructive admin actions before they execute. | DONE (2026-07-30, browser-verified) | R2 | Audited every admin destructive action. Product delete, user delete, and order refund already had `AlertDialog` confirmation (pre-existing). Found and closed one real gap: changing an order's status to "cancelled" (restores stock + emails the customer) fired immediately from a plain `<select>` with no confirmation, on both `AdminOrdersPage.js` (inline table select) and `AdminOrderDetailPage.js` (detail page select). Both now intercept `"cancelled"` and open an `AlertDialog` first; the underlying `<select>` is prop-controlled so it visually snaps back to the real status if the admin clicks "Keep Order". Verified live: selecting Cancelled opens the dialog, "Keep Order" reverts with no mutation sent. |
| REQ-1603 | Replace the homepage testimonials' copy — verified to be unmodified Flowbite/Tailwind template boilerplate literally about "Figma components" and "Tailwind CSS utility classes," unrelated to an ebook store — with content grounded in CodeBook's actual catalog. | DONE (2026-07-30) | R0 | `src/pages/Home/components/Testimonials.js`: 4 testimonials rewritten to reference real seeded products (React, backend, Git, JavaScript books) with plausible student/engineer personas; same layout/images/structure, only copy changed. |
| REQ-1604 | Replace the homepage FAQ's Lorem-ipsum answers with real answers grounded in this app's actual behavior (digital delivery via Dashboard, Stripe-powered international payments, ticket-based refunds). Also fixes a typo ("Internation" → "International"). | DONE (2026-07-30) | R0 | `src/pages/Home/components/Faq.js`. Confirmed via `grep -rl "Lorem ipsum" src` that zero placeholder text remains anywhere in `src/`. |
| REQ-1605 | Add a Lucide icon pass across every title/label/button/filter that currently has no icon, with meaningful per-context colors. | IN-PROGRESS (2026-07-30, partial) | R1 | `lucide-react` installed and used for the new KPI card icons (Dashboard + Analytics summary rows). A full sweep replacing/augmenting every remaining `bi-*` icon site was explicitly not attempted this round — see REQUIREMENTS §16xx notes and STATE.md for the reasoning (avoid mixing two inconsistent icon systems under time pressure). Remaining scope: sidebar nav, filter bars, empty states, page headers — needs explicit direction (extend vs. full migration) before continuing. |
| REQ-1606 | Reusable scroll/stagger reveal animation system (`docs/PARALLAX_SCROLL_REVEAL_SYSTEM.md`, React-ported to plain JS since strict TS is Phase 2) — apply staggered field-by-field reveal to Login/Register, and a per-route fade/slide-in on every page navigation. | DONE (2026-07-30, browser-verified) | R1 | New `src/components/ui/motion.js` (`ScrollReveal`, `StaggerContainer`/`StaggerItem`, `revealVariants`) using `framer-motion` (new dependency), all respecting `prefers-reduced-motion`. Applied to `Login.js`, `Register.js` (stagger), and `App.js` (per-route `key={location.pathname}` fade+slide, enter-only — no exit, to avoid React Router's AnimatePresence-needs-explicit-`location` complexity). **Real bug found and fixed during browser verification**: a `StaggerItem` that mounts asynchronously (Login's demo-account dropdown, gated on `useDemoAccounts()` resolving) never animated in — it inherited "hidden" forever because Framer Motion's variant propagation only replays a parent's *already-fired* transition for children present at that time, not ones added later. Fixed by using the independent `ScrollReveal` (mounts-and-triggers-itself) instead of `StaggerItem` (depends-on-parent-orchestration) for that one async-gated block. Caught via an actual screenshot, not just a clean build — the compiled output gave no signal that the section was invisible. |
| REQ-1607 | `RippleButton` reusable component (`docs/RIPPLE_BUTTON_EFFECT.md`) — click ripple + optional CTA shine sweep, applied to primary CTAs. | DONE (2026-07-30, build-verified) | R0 | `src/components/ui/ripple-button.js` (pure CSS animation, no framer-motion needed for this one — cheaper). `ripple`/`cta-shine` keyframes added to `tailwind.config.js`; `.cta-shine-wrap` sweep class in `index.css`. Applied to the Login/Register submit buttons. Not yet applied to every button in the app (Add to Cart, admin action buttons, etc.) — component is ready for that as a follow-up, scoped down same as the icon pass. |
| REQ-1608 | Admin Dashboard + Analytics KPI cards restyled per `docs/UI_STYLING_GUIDE.md`'s gradient/colored-border card system (adapted from that doc's dark-glassmorphism base to this app's light-first theme), with icon badges and breakdown pill badges — matching the visual density of the user's `stock-inventory` reference screenshots. | DONE (2026-07-30, browser-verified) | R1 | Rewrote `AdminMetricsCard` (6 color variants: sky/emerald/amber/rose/violet/blue) with a `breakdown` prop for pill badges; wired real `ordersByStatus` data into the Total Orders card's breakdown (no fabricated stats — cards without real breakdown data don't get placeholder pills). Applied to `AdminDashboardPage.js` (4 top cards + Average Order Value) and `AdminAnalyticsPage.js`'s summary row. Full-page screenshot confirms the visual match. Deeper nested stat blocks further down the Analytics page (sales trend breakdowns, per-product tables) were not converted — scope was the primary KPI rows. |
| REQ-1609 | Rotating hero background with crossfade + Ken Burns zoom (`docs/HERO_ROTATING_BACKGROUND_SPEC.md`, originally vanilla-JS/DOM — ported to React state instead of direct DOM manipulation). | DONE (2026-07-30, build + visual-verified) | R0 | `src/pages/Home/components/HeroRotatingBackground.js` + CSS in `index.css` (`.hero-rotating-bg__layer`, `hero-bg-kenburns-cycle` keyframes). Kept the existing two-column Hero layout exactly as-is — only the single static image slot became a 2-layer rotating background, not a full redesign into a full-bleed hero (that would have been a much bigger, unrequested layout change). `prefers-reduced-motion` falls back to a plain timed crossfade with no zoom. Simplified vs. the full spec: no per-slide URL-fallback probing, advance is timer-based (`setInterval` matched to the 14s Ken Burns duration) rather of `animationend`-driven — a reasonable scope reduction given 4 known-good Unsplash URLs, not user-uploaded content that could 404. |
| REQ-1610 | Make product/order/user references clickable (sky-colored links to the relevant detail page) everywhere they appear as plain text in admin tables. | DONE (2026-07-30, browser-verified) | R1 | Audited every admin list page. Found 3 real gaps: `AdminOrdersPage.js` (Order ID and Customer name were plain text — only a separate small icon button linked to the detail page), `AdminProductsPage.js` (product name was plain text), `AdminReviewsPage.js` (worse: showed a raw truncated product UUID with no product-name lookup at all, and no link on the reviewer's name). Fixed all three — Order ID/Customer now link to `/admin/orders/:id` / `/admin/users/:id`; product name links to `/admin/products/:id`; reviews now resolve the product name via a `productId → name` lookup built from `useAllProducts()` and link both the product and the reviewer. `AdminUsersPage.js` already had a working row-level click-through (pre-existing, unchanged). Not yet audited: `AdminTicketsPage.js`, `AdminHistoryPage.js`, and the customer-facing `DashboardPage.js` order list — flagged as remaining scope, not confirmed broken or fixed. |
| REQ-1611 | TanStack Table migration (dropdown row actions instead of inline buttons, read-only list pages, search+filter row) across all admin list pages, matching the user's `stock-inventory` reference project's pattern. | NOT STARTED (2026-07-30) | R2 | `@tanstack/react-table` installed as a dependency, ready to use. Explicitly not started this round — a full, correct migration across ~8 admin list pages (Products, Orders, Users, Reviews, Tickets, History) touching sorting/filtering/pagination logic on every one is a large, separate initiative that risks regressions if rushed alongside everything else done this round. The existing `SortableTable` component works correctly and was just made more consistent (REQ-1610). Needs its own dedicated pass. |
| REQ-1612 | Auto-generate an invoice PDF, attach it, and email it to the customer automatically on successful order/payment. | NOT STARTED (2026-07-30) | R2 | No PDF-generation library or invoice template exists yet in `backend/`. Needs scoping (invoice numbering scheme, line-item/tax layout, which library — e.g. `pdfkit` or `puppeteer`-based HTML-to-PDF) before implementation; deferred rather than rushed into the existing `email.service.js`/Brevo flow. |
| REQ-1613 | Multi-model AI statistics/prediction insights on the Business Insights / Analytics page, with automatic fallback across providers (`docs/LLM_MODEL_SELECTION.md` — merged single reference doc, formerly split across `LLM_MODEL_SELECTION.md` + `LLM_MODEL_IMPLEMENTATION_GUIDE.md`). | DONE (2026-07-31, browser-verified with live providers) | R1 | User supplied 9 free-tier provider keys in `docs/personal-dev-info.txt` (2026-07-31). Adapted rather than copy-pasted the two reference docs (both assume Next.js API routes + Redis sessions): built `backend/src/lib/ai/{types,providers,client,index}.ts` — a generic OpenAI-compatible chat-completion client shared by 9 providers (Gemini, Groq, OpenRouter, Cerebras, NVIDIA NIM, Mistral, Cohere, GitHub Models, Hugging Face), tried in priority order, silently skipping unconfigured/failing providers (docs/LLM_MODEL_SELECTION.md's "never depend on a single model"). `backend/src/services/aiInsights.service.ts` + `POST /admin/ai-insights` route take a client-computed analytics text summary (zero extra DB queries — reuses data already in the TanStack Query cache, mirroring this project's existing "analytics computed client-side" pattern) and return 3-5 short recommendations, with a 10-minute in-memory response cache (no Redis, per REQ-1403) keyed by summary hash so repeat dashboard visits never re-trigger a provider call. Frontend: `useAiInsights` hook (`src/hooks/useAdmin.ts`) + new "AI Business Insights" card on `AdminAnalyticsPage.tsx` with loading-pulse/error/not-configured states and a manual Refresh button. Deliberately did NOT wire: Cloudflare Workers AI (needs an account ID not present in the provided info), any speech/image/embedding key (ElevenLabs/Replicate/Fal/Jina — out of scope for a text-insights feature), or any key that would create a second parallel architecture next to already-decided choices (Clerk/Auth0 vs. Google OAuth; Contentful vs. no CMS) — see DECISION_LOG.md. **Real bug caught during verification:** the initial `max_tokens: 400` truncated Gemini 2.5 Flash's response mid-sentence because reasoning-capable models spend part of the token budget on hidden reasoning before the visible answer; fixed by raising to `max_tokens: 1024`. Verified live against the real seeded DB: Gemini returned 4 well-formed, data-grounded recommendations (reorder low stock, promote best-seller, re-engage new users, cross-sell), cache-hit confirmed on repeat call, 0 console errors, `tsc -b`/`eslint` clean on both frontend and backend. |
| REQ-1614 | `docs/SAFE_IMAGE_REUSABLE_COMPONENT.md` — evaluate for adoption. | N/A — not applicable (2026-07-30) | R0 | The doc is explicitly a `next/image`-with-`<img>`-fallback pattern for Next.js App Router projects. This project is CRA and has never used `next/image` or any image optimizer — every image is already a plain `<img>`, so there is nothing for this pattern to fall back *from*. No code change; documented here so it's not silently missed. |
| REQ-1615 | `docs/VERCEL_PRODUCTION_GUARDRAILS.md` — bot protection, security headers, caching, `robots.txt`. | DEFERRED (2026-07-30) | R1 | Doc is Next.js-specific (`next.config.ts`, `app/robots.ts`); this project's frontend is a CRA SPA on `vercel.json`. Low priority because the project isn't deployed/receiving production traffic yet (Phase 1 backend migration hasn't shipped). Worth a lightweight adaptation (security headers + `robots.txt` in `vercel.json`) once deployment is back in scope — not before. |
| REQ-1616 | Product catalog data-enrichment pass, phase 1 of a broader "redesign every page with richer data" ask (user: "add genuinely new data fields... everything, all pages"), inspired by the `stock-inventory` reference project. | DONE (2026-07-31, browser-verified) | R1 | Extended `Product` with 8 new fields: `sku`, `isbn`, `publisher`, `publishedYear`, `language`, `edition`, `fileFormat`, `tags` (String[]). Deliberately scoped to digital-catalog-appropriate metadata, not literal physical-inventory fields (weight/dimensions/warehouse) — the storefront's own FAQ confirms every title is a pure digital download via the Dashboard, never physically shipped, so those would have been internally inconsistent fake data. `prisma db push --accept-data-loss` (safe: `sku` is nullable, no existing duplicates). All 15 seed products hand-enriched with real, grounded values (not filler) in `data/db.json` and reseeded. Wired through the backend Zod schema + `createProduct`/`updateProduct`'s field-list constants, the frontend `Product` type, a new "Book Details" section on the customer `ProductDetail.tsx`, a new "Catalog & Inventory Metadata" card on `AdminProductDetailPage.tsx`, and matching inputs in `ProductForm.tsx` (create + edit). **Real bug caught during verification:** `product.tags.length` crashed with `TypeError: Cannot read properties of undefined` on first load — root cause was the existing localStorage-persisted TanStack Query cache (REQ-1310) still holding pre-migration product shapes without the new `tags` field; `product.tags` was typed as always-present but wasn't at runtime. Fixed two ways: (1) made `tags` properly optional/nullable in the frontend type and defensive (`?.`, `|| []`) at every read site — honest typing given real-world cache/data drift; (2) bumped `package.json` version (0.2.1 → 0.2.2), which the existing `__APP_VERSION__`-keyed persisted-cache `buster` (REQ-1310) picks up automatically to discard the stale cached shape on next load — confirmed live (edit form fields empty before the bump, correctly populated after). Verified end-to-end: `tsc -b`/`eslint` clean on frontend+backend, and a live browser edit→save→revisit round-trip (changed a field, saved, confirmed the new value via the live API, reverted the test edit). |
| REQ-1617 | Customer-facing Order Detail page (itemized breakdown + shipment/status timeline) and clickable order history, phase 2 of the page-redesign initiative. | DONE (2026-07-31, browser-verified) | R1 | This store never had a customer-facing single-order route — order data only existed as inline cards on the Dashboard. Added `GET /orders/:id` (ownership-checked: the order's own user, or an admin) that embeds a chronological status-change `timeline` in the same response, avoiding a second round-trip — the timeline is derived entirely from the `ActivityLog` rows every existing admin order action (status update, tracking add, label generate, refund) already writes (`entityType: "order"`, `entityId`), so **no new schema was needed**; extended `activityLog.service.ts`'s filter with `entityId` plus a small `getOrderActivityTimeline()` helper. New `src/pages/Order/OrderDetailPage.tsx` at route `/orders/:id`: order summary, itemized line-item breakdown (this store has no separate tax/shipping line — `amount_paid` is the exact sum of cartList line totals, so the "itemized breakdown" is the real cartList data rendered properly, not invented figures), and the timeline with per-status icons. `DashboardCard.tsx`'s Order ID badge is now a real link to the new page (previously plain text) — the other REQ-1610-style clickable links (product/user) were already in place. `invalidateAfterOrderStatusUpdate()` extended with an `orderId` param so any admin order mutation also invalidates the customer-facing `["order", id]` cache. **Naming collision caught during verification:** a customer-facing `useOrder`/`getOrderById` would have collided with the pre-existing *admin* `useOrder` (`useAdmin.ts`) and `getOrderById` (`adminService.ts`) — different behavior (ownership-checked vs. admin-can-view-any), same names, both re-exported from the `services` barrel. Renamed to `useOrderDetail`/`getOrderDetail` before it could ship as a silent bug (TypeScript caught the barrel-export collision at compile time). Verified live: logged in as the demo customer account, clicked the order link from the Dashboard, confirmed a real timeline entry ("Order placed" → "Order marked processing") rendered from actual `ActivityLog` data (not fabricated), 0 console errors, `tsc -b`/`eslint` clean on both frontend and backend. |
| REQ-1618 | Customer address book (User Detail phase 3: "address book, order history") plus admin-side visibility into a user's saved addresses and full order history on `AdminUserDetailPage.tsx` — user's next explicit continuation instruction after REQ-1617. | DONE (2026-07-31, browser-verified) | R1 | Real gap found while scoping: `shipping.service.ts`'s Shippo label generation already had a `toAddress` code path reading `order.shippingAddress`/`order.address`, but nothing in the app ever wrote either — checkout has no address collection (`StripeCheckout.tsx`'s own on-screen note: "we haven't implemented user address collection during checkout... using fallback test addresses"). An address book is a genuine fix for that gap, not a fabricated feature. New `Address` model (`userId`, `label`, `fullName`, `street1/2`, `city`, `state`, `zip`, `country`, `phone`, `isDefault`) with a `User.addresses` relation; single-default-per-user invariant enforced in the service layer (`addresses.service.ts`), not a DB constraint — Prisma has no partial-unique-index here. Self-service CRUD at `/addresses` (`requireAuth`, ownership always derived from the JWT's `req.user.id`, never a client-supplied `userId` — prevents IDOR). `GET /admin/users/:id` extended to embed `orders` + `addresses` in one response (reuses existing `getOrdersByUserId`), fixing `AdminUserDetailPage.tsx`'s stale doc-comment claim ("Displays full user information including orders") that was never actually true. Customer-facing UI: new `AddressBook.tsx` on the Dashboard — inline add/edit form (no modal, no extra route, consistent with the rest of this codebase's CRUD patterns), `AlertDialog` delete confirmation, matching `ticketService.ts`'s established service-file pattern. Admin UI: read-only "Address Book" + "Order History" cards on `AdminUserDetailPage.tsx`, order rows linking to `/admin/orders/:id`. Deliberately did NOT wire checkout to collect/select an address — that's a separate, higher-risk checkout-flow change (payment path) outside this round's ask; flagged as a natural follow-up now that real address data can exist. Verified live end-to-end as both roles: created/edited/deleted an address as the demo customer (each op instant, correct toast, no refresh, first address auto-defaulted), confirmed it appeared on the admin's user-detail view under both new cards, clicked through an order-history row to the real admin order detail page, `tsc -b`/`eslint` clean on both frontend and backend (0 new errors, only the same 7 pre-existing warnings). |
| REQ-1619 | Tickets + Reviews data-enrichment pass, final two domains of the page-redesign initiative (user: "continue" after confirming REQ-1618 and naming Tickets/Reviews as the remaining scope). | DONE (2026-08-01, browser-verified) | R1 | **Tickets**: added `priority` (low/medium/high/urgent, default medium), `category` (billing/technical/refund/account/other, default other), and an optional `orderId` link to `Ticket` — all three are genuinely useful triage fields for a real support-ticket system, and `orderId` is directly grounded in the storefront's own FAQ ("open a support ticket from your Dashboard" for refunds). `orderId` is ownership-checked server-side (a ticket may only reference an order that actually belongs to the reporting user — same IDOR-prevention pattern as `reviews.routes.ts`'s existing order check). New `PUT /tickets/:ticketId/priority` (admin-only), wired through `CreateTicketPage.tsx` (category/priority selects + an optional "Related Order" dropdown sourced from the user's real orders), `TicketDetailPage.tsx` (priority badge for customers, editable dropdown for admins, order link), `TicketsListPage.tsx` (priority badge, only shown when non-default), and `AdminTicketsPage.tsx` (new Priority column + filter). **Reviews**: added `adminReply`/`adminReplyAt` to `Review` — a public "store response," the standard e-commerce seller-reply-to-review pattern — plus a computed-not-stored "Verified Purchase" badge (every review already requires a real `orderId` at creation per `reviews.routes.ts`, so this is always true, zero schema needed for it). New `GET /admin/reviews/:id` + `AdminReviewDetailPage.tsx` at `/admin/reviews/:id` — Reviews was the one admin domain with a list page but no detail page (Products/Orders/Users/Tickets all already had one); row-click on `AdminReviewsPage.tsx` now navigates there, with `stopPropagation` added to the existing Link/status cells to avoid double-navigation. Reply form shows "Post Response"/"Update Response", disabled when unchanged; clearing the textarea and saving removes an existing reply. `ReviewCard.tsx` renders the Verified Purchase badge and, when present, the store response in a distinct "Response from CodeBook" callout. **Real latent bug caught and fixed while implementing this**: `AdminUserDetailPage.tsx` (REQ-1618) read `user.addresses.length`/`user.orders.length` directly — a persisted query-cache entry from before REQ-1618 shipped would crash on that exact `product.tags`-class bug from REQ-1616; fixed with local `addresses = user?.addresses \|\| []` / `orders = user?.orders \|\| []` fallbacks and relaxed `AdminUserDetail.orders`/`addresses` to optional in the type. `package.json` bumped 0.2.2→0.2.3 to trigger the existing `__APP_VERSION__` persisted-cache buster for the new required `Ticket.priority`/`category` fields. Verified live end-to-end: created a real ticket with category/priority/linked-order as the demo customer, changed its priority as the demo admin (instant toast + UI update), posted a store reply to a seeded review and confirmed it rendered on the live customer-facing product page alongside the Verified Purchase badge, 0 console errors, `tsc -b`/`eslint` clean on both frontend and backend. |
| REQ-1620 | Wire checkout to the address book — the one item deliberately deferred from REQ-1618 — plus surface the saved address everywhere an order's shipping info is shown. | DONE (2026-08-01, browser-verified) | R1 | New `Order.shippingAddress Json?` column: a snapshot (not a live relation) of the customer's selected saved `Address` at checkout time, taken deliberately as a copy so the order keeps its own record even if the source address is later edited/deleted. `StripeCheckout.tsx` gained an optional "Shipping Address" selector (defaults to the user's default address, `<select>` of all saved addresses, "None" always available) — never blocks checkout, since this storefront delivers digital downloads; the selected address rides through `PaymentSuccessPage.tsx`'s `createOrder()` call. Backend: `createOrderSchema` extended with an optional `shippingAddressSchema`, stored as-is on the `Order` row. This also closes the real pre-existing gap noted in REQ-1618: `shipping.service.ts`'s Shippo label generation already read `order.shippingAddress` but nothing ever populated it — real addresses now flow through automatically (fixed a naming mismatch, `shippingAddress.fullName` vs. the code's `shippingAddress.name` read, so the real field is actually picked up). Surfaced the address on: `OrderTrackingInfo` (shared component — restructured to show address independent of shipment-tracking status, since a shipping address is set at order time, not shipment time), and `AdminOrderDetailPage.tsx` (own "Shipping & Tracking" card, so the admin sees the same address Shippo will use before generating a label). Deleted `src/pages/Cart/components/Checkout.tsx` — a completely unreferenced, superseded mock-payment component (hardcoded fake card fields, non-functional submit) found dead during this work, confirmed zero imports anywhere. Verified live end-to-end: added a real address, went through a full Stripe test-mode payment with that address selected, confirmed it landed on the created order and rendered correctly on both the customer order-detail page and the admin order-detail page, 0 console errors. |
| REQ-1621 | Instant-navigation / cache-freshness pass across the whole app (user: "instant ui update invalidation... no revert old data... instant navigation... no unnecessary duplicate api calls"), plus the two remaining Phase-4 partial items (RippleButton rollout, KPI card conversion). Architecture note: user's ask referenced Next.js `page.tsx`/`"use client"`/SSR concepts that don't apply to this Vite SPA (no server-rendering layer) — translated to this app's real architecture (TanStack Query cache + client-side routing) instead of attempted literally. | DONE (2026-08-01, browser-verified) | R1 | **Cache-freshness audit**: found and fixed 2 real invalidation gaps — order creation and order-status/tracking/label/refund mutations didn't invalidate `["admin-user", userId]`, which embeds that user's `orders` (REQ-1618), so an admin viewing a user's detail page in one tab wouldn't see an order change made in another; also extended the 3 address mutations (`useCreateAddress`/`useUpdateAddress`/`useDeleteAddress`) to invalidate the same key for the same reason. Fixed a real `window.location.reload()` anti-pattern in `AdminReviewsPage.tsx`'s error-retry (→ the query's own `refetch()`) — the only other 2 occurrences in the codebase (`ErrorBoundary`, `GoogleSignInButton`) are legitimate (crash recovery / leaving the SPA for Google's OAuth redirect). Confirmed no other full-reload navigation exists anywhere — this is a client-side-routed SPA throughout, so internal navigation was already instant by construction; the gap was stale *data* after mutations, not page reloads. **Hover-prefetch**: new `usePrefetchOnHover` hook (debounced, deduped `queryClient.prefetchQuery`) plus a new `onRowHover` prop on the shared `DataTable` (REQ-1611) component. Wired two techniques depending on what's already known: (1) direct `queryClient.setQueryData` seeding — zero network cost — for `ProductCard` (customer product list → detail) and the Admin Orders/Products/Tickets/Reviews list rows, since those lists already hold the exact same shape the detail page's query expects; (2) real network-based `prefetchQuery` for `AdminUsersPage` (list rows lack the `orders`/`addresses` the detail view embeds) and Dashboard order cards (list lacks the order-detail `timeline`) — seeding those with incomplete data would have both flashed missing fields and suppressed the real fetch (since the query would look "fresh"). Verified live: hovering an admin user row fires a real `GET /admin/users/:id` before any click (confirmed via network trace), and the detail page then renders instantly with zero loading state. **Dead/duplicate code**: full file-level unused-export sweep across `src/{components,pages,hooks,services,utils,lib}` and `backend/src` (grep-based reference-count check) — found and removed the dead `Checkout.tsx` (see REQ-1620); everything else was genuinely referenced. Extracted a new shared `AddressLines` component (`src/components/ui/address-lines.tsx`) — the same 4-line name/street/city-state-zip/country rendering had been hand-duplicated across `AddressBook.tsx`, `AdminUserDetailPage.tsx`, `OrderTrackingInfo`, and `AdminOrderDetailPage.tsx`. **REQ-1607 (RippleButton rollout)**: applied to every primary-CTA submit/action button on the purchase + engagement funnel: `ProductCard`'s Add to Cart, `CartList`'s Place Order, `StripeCheckout`'s Pay Now, `CreateTicketPage`'s Create Ticket, `ReviewForm`'s Submit/Update Review, admin `ProductForm`'s Create/Update Product, `AdminReviewDetailPage`'s Post/Update Response, `AddressBook`'s Add/Save Address — secondary Cancel buttons deliberately left plain, matching the Login/Register precedent. **REQ-1608 (KPI card conversion)**: converted the previously-plain "Average Order Value"/"Products Sold" and "User Analytics" (Total/Active/New) stat blocks on `AdminAnalyticsPage.tsx` to the gradient `AdminMetricsCard` system; the "Product Performance" mini-stats (nested inside an existing Card, where a second full gradient card would have looked over-nested) got a lighter colored-icon-badge treatment instead, consistent iconography without visual over-nesting. Verified live: `tsc -b`/`eslint` clean on both frontend and backend throughout (0 errors, only the same 7 pre-existing accepted warnings), full browser pass across the affected pages, 0 console errors. |
| REQ-1622 | Build-artifact/config-hygiene audit (user: "check .gitignore properly", "merge/delete unused files/folders"). | DONE (2026-08-01, verified) | R1 | Found `vite.config.js` + `vite.config.d.ts` + 3 `.tsbuildinfo` files all committed to git — a real misconfiguration, not intentional: `tsconfig.node.json` (composite, no output redirect) let `tsc -b` emit compiled duplicates of `vite.config.ts` straight into the repo root. Root-caused instead of just deleting the symptoms: `tsc -b` refuses `noEmit` on a composite project (`TS6310`, confirmed against the installed TS 5.9.3), so redirected both the node-project and root-project `tsBuildInfoFile`/`outDir` into `node_modules/.tsbuildcache/` (already gitignored) — `tsc -b` now produces zero stray root-level artifacts. Deleted the 5 already-committed files, added `*.tsbuildinfo` to `.gitignore` as a backstop. Investigated `public/logo.png`/`logo192.png`/`logo512.png`/`manifest.json` (user asked "why do we still have these, it's Vite now") — confirmed via grep these are **not** dead CRA scaffolding: actively referenced by `index.html` (`og:image`, `twitter:image`, `apple-touch-icon`, `<link rel="manifest">`) and 3 live components (`Header.tsx`, `AdminSidebar.tsx`, `PageNotFound.tsx`), independent of any build tool — kept, not removed. Left `aws-lambda/` (the retired 33-function legacy backend) untouched — it's already excluded from lint scope and was a deliberate "keep for reference" decision from the CRA→Vite/Lambda→Express migration (REQ-1213), not an oversight to silently reverse. |
| REQ-1623 | De-duplicate the `LAMBDA_API_BASE`/`API_BASE` backend-URL constant, hand-copied with an inconsistent (and dead) AWS Lambda fallback across 13 files (user: "no similar, duplicate, redundant things... no unnecessary duplicate api call"). | DONE (2026-08-01, verified) | R1 | Every `src/services/*.ts` file plus `GoogleSignInButton.tsx` independently declared `const LAMBDA_API_BASE = import.meta.env.VITE_LAMBDA_API_URL \|\| "https://d4vvkswb4a.execute-api.eu-north-1.amazonaws.com"` — a real latent bug, not just duplication: that AWS API Gateway endpoint is the pre-migration backend, scheduled to stop responding entirely on 2026-08-16 (`docs/PROJECT_PLAN.md` trigger date). If `VITE_LAMBDA_API_URL` were ever unset (fresh clone, misconfigured deploy target), every request would silently target a dead endpoint instead of the real Express backend. One file, `reviewService.ts`, had a third variant (`\|\| ""`) — inconsistent with the rest. Consolidated into a single `src/lib/apiBase.ts` exporting `API_BASE_URL`, falling back to `http://localhost:3000` (the documented local-dev backend port, matching `.env.example`) instead of the dead AWS URL. All 13 call sites now import the one constant. |
| REQ-1624 | Backend auth hardening (user: "harden the auth system... include other secured mechanism if possible"). | DONE (2026-08-01, verified) | R1 | Added `helmet` (standard security headers — HSTS, `X-Content-Type-Options`, `X-Frame-Options`; `crossOriginResourcePolicy` explicitly set to `cross-origin` since this API is intentionally consumed by a separate frontend origin) and `express-rate-limit` (`authLimiter`: 20 requests/15min, applied to `POST /login`, `POST /register`, `POST /auth/demo-login` — not the read-only demo-accounts list or the Google OAuth redirect/callback, which don't accept a client-supplied credential to brute-force). Added fail-fast startup validation in `lib/auth.ts`: refuses to start in production if `JWT_SECRET` is missing, under 32 characters, or still one of the two known placeholder strings, with a message pointing at `openssl rand -base64 64`. Live-verified: the already-running dev backend (`tsx watch`) hot-reloaded on save and immediately started returning the new `Strict-Transport-Security`/`X-Content-Type-Options`/`X-Frame-Options` headers. **User's "same secret base64 in backend and frontend" framing needs a correction, not a literal implementation**: a JWT signing secret must never exist in frontend code — anything shipped in a browser bundle is public, so a "shared secret" there would let anyone forge valid tokens. The frontend already correctly holds only the *token* it receives after a successful login (not the secret that signs it); documented this boundary in `backend/.env.example`. |
| REQ-1625 | Redesign Login/Register as a professional 60/40 image+form split screen (user: reference `stock-inventory`/`calendar-appointment` codebases' auth UI style, `public/images/10011.avif` for Login and `10003.avif` for Register, "professional... real world"). | DONE (2026-08-01, browser-verified) | R1 | Read the two reference projects' actual Login components (`stock-inventory/components/Pages/LoginPage.tsx` + its `AuthPageShell`/`AuthInfoPanel` primitives; `calendar-appointment/src/components/login/Login.tsx`) to extract the underlying *pattern* — full-bleed image/gradient panel with staggered brand copy on one side, a card-contained form on the other — rather than copying either repo's actual Tailwind classes, since both are Next.js projects with a different (dark-glassmorphism) design system than this one. New reusable `AuthSplitLayout` (`src/components/ui/auth-split-layout.tsx`) built entirely from this project's own existing light/dark tokens (`bg-white dark:bg-gray-800`, `border-gray-200 dark:border-gray-700`, `rounded-lg` — the same tokens the shared `Card` component already uses), not the admin console's separate dark-glass system, since Login/Register are customer-facing pages that already used the light/dark-toggle scheme. Image panel is `hidden lg:block` (60% width) with a bottom gradient overlay for legible overlaid copy + 3 feature bullets per page; form panel (40%) reuses the existing `FormInput`/`FormLabel` shared components instead of hand-rolled `<input>`/`<label>` JSX. Converted `Login.tsx`'s email/password from uncontrolled refs to controlled `useState`, matching `FormInput`'s existing controlled-only contract (already the established pattern in `ProductForm.tsx`/`AdminUserEditPage.tsx`) — `Register.tsx` kept its existing uncontrolled `form.elements` pattern unchanged since `FormInput` supports that too (`name` attribute is enough). All existing behavior preserved: demo-account dropdown, Google sign-in, RippleButton, Sonner error toasts, cache invalidation on success. Verified live: real demo-account login flow end-to-end (0 console errors), both pages screenshotted at desktop/mobile/dark-mode — image panel correctly disappears below `lg`, dark mode tokens render correctly. |
| REQ-1626 | Fix page-width alignment inconsistency (user, with screenshot: Login card sat visibly narrower/offset from Header/Footer at wide viewports, "navbar footer page layout all should use the same css"). | DONE (2026-08-01, browser-verified) | R1 | Root cause was two-fold, both found via direct DOM measurement (`boundingBox()` comparison), not guessing from screenshots alone: (1) `AuthSplitLayout` (REQ-1625) had its own `mx-auto max-w-6xl` nested inside the App.tsx `max-w-9xl` wrapper (REQ-1622-1625 round) — removed, width now comes solely from the shared wrapper, matching Header/Footer exactly. (2) A **legacy global CSS rule** in `index.css` — `main { max-width: 1280px; margin: auto; padding: 15px; }`, predating this session's Tailwind-based centralized width system — was silently capping every bare `<main>` tag site-wide (12 files: Home, Products, Cart, Dashboard, ProductDetail, PageNotFound, AuthCallback, OrderPage, and AuthSplitLayout itself), overriding the intended `max-w-9xl` alignment. Removed the rule entirely; its `min-height: 90vh` (sticky-footer behavior) was replaced with the standard flex pattern instead (`App.tsx`'s root now `flex min-h-screen flex-col`, the routed-content wrapper `flex-1`) so short-content pages still push the footer to the bottom without depending on a bare-tag CSS selector. Verified via real DOM measurements at a 2000px viewport (matching the user's screenshot): Header/page-content/Footer all now measure identical `x`/`width` (224.5px / 1536px), and visually confirmed unchanged on Home/Products/Admin (no regression from the padding/min-height removal). |
| REQ-1627 | Remove genuine CRA-only leftovers now that the project is Vite-only (user: "remove all only CRA react things... only keep react vite things which actually using in the codebase"). | DONE (2026-08-01, verified) | R1 | Audited for CRA scaffold files (`jsconfig.json`, `craco.config.js`, `react-app-env.d.ts`, `setupTests`, `reportWebVitals`, `serviceWorker`, `.eslintrc*`, `react-scripts`, `%PUBLIC_URL%`) — all already absent from earlier migration rounds, nothing found. Found and fixed two real remnants: the `web-vitals` npm package was still declared in `package.json` with zero actual imports anywhere in `src/` (dead since `reportWebVitals.js`, its only consumer, was deleted in an earlier round) — removed. `ProductDetail.tsx` had a stale doc comment referencing `REACT_APP_BASE_URL` while the code beneath it correctly used `VITE_BASE_URL` — comment corrected. `public/logo*.png`/`manifest.json` re-confirmed (again) as actively used, non-CRA-specific static assets, not removed. |
| REQ-1628 | Sentry error monitoring with an ad-blocker-safe tunnel endpoint (user: real Sentry project credentials supplied in chat; "in production I see this in normal browser with addons sentry blocks... use sentry endpoint tunneling as /api/monitoring"). | DONE (2026-08-01, browser-verified against real Sentry project) | R2 | `docs/Redis_Sentry_PostHog_INTEGRATION_GUIDE.md` is entirely Next.js-specific (`@sentry/nextjs`, `next.config.js`, `app/layout.tsx`, `instrumentation.ts`, edge runtime) — read it for the underlying pattern, translated to this project's real stack: `@sentry/react` (frontend) + `@sentry/node` (backend Express), not the Next.js packages. Scope narrowed to exactly what the user asked for this round (Sentry only — Redis/PostHog from the same doc were not requested). Backend: `backend/src/lib/sentry.ts` (`Sentry.init`, no-ops safely if `SENTRY_DSN` unset) + `backend/src/routes/monitoring.routes.ts` — `POST /api/monitoring`, rate-limited (60/min), parses the DSN out of the incoming envelope's own header line and forwards the raw envelope server-side to Sentry's real ingest URL, mounted with `express.text()` (raw body, same pattern as the existing Stripe webhook route) ahead of the global `express.json()`. `Sentry.setupExpressErrorHandler(app)` wired after all routes to capture backend errors too. Frontend: `src/lib/sentry.ts` initializes with `tunnel: `${API_BASE_URL}/api/monitoring`` so the browser SDK never contacts `*.ingest.sentry.io` directly; wired into the existing `ErrorBoundary.tsx`'s `componentDidCatch` (reused, not duplicated). `@sentry/vite-plugin` added to `vite.config.ts` for production source-map upload, using `loadEnv` to read `SENTRY_ORG`/`PROJECT`/`AUTH_TOKEN` (no `VITE_` prefix — build-tool-only, never shipped to the client bundle), self-disabling when the token isn't set. Verified live against the real Sentry project end-to-end: (1) a raw curl-crafted envelope through `/api/monitoring` returned the real Sentry ingest response; (2) a browser-triggered `Sentry.captureMessage()` produced `POST /api/monitoring → 200` in the network log with **zero** requests to any `sentry.io` domain, confirming ad-blocker-safety; (3) `npm run build` uploaded real source maps to the Sentry project (visible in the build's own upload report), confirming the auth token and org/project slugs are valid. |
| REQ-1629 | Data-loss discovery and recovery: `public/manifest.json`, `logo192.png`, `logo512.png` had silently disappeared from the working tree (cause undetermined) while git's index still held them staged from an earlier `git add -A`, surfacing as a real, user-visible `Manifest: Syntax error` console error. | DONE (2026-08-01, verified) | R1 | Found while investigating the layout-alignment report — checked the browser console and saw the manifest error. `git status` showed all three as `RD` (staged rename, then deleted from disk) rather than a normal edit, and `git show HEAD:public/manifest.json` confirmed no prior commit ever had it (this session's whole `public/` tree exists only via the still-uncommitted `git add -A` from earlier rounds), so the only recovery path was git's index, not history. Restored via `git show :public/manifest.json > ...` (text) and `git checkout-index -- ...` (binary PNGs) — both came back byte-identical to what REQ-1622's earlier audit had already verified as correct. Swept the rest of the repo for other `RD` entries afterward; none found — isolated to these 3 files. |
| REQ-1630 | Post-Sentry deep-audit pass (user: "deep audit... any misconfig, mismatch, any gaps"). | DONE (2026-08-01, verified) | R0 | `depcheck` found `@testing-library/jest-dom`/`react`/`user-event` as genuinely dead devDependencies — zero test files exist anywhere in `src/`, zero imports (leftover from the original CRA scaffold, missed by REQ-1627's cleanup since that pass only checked for CRA config files, not dead test-only deps). Uninstalled. Separately, cross-checked every `import.meta.env.VITE_*`/`process.env.*` read in the codebase against both `.env.example` files: frontend was missing docs for `VITE_BASE_URL`/`VITE_STRIPE_PUB_KEY`/`VITE_IMAGE_SERVICE`/`VITE_CLOUDINARY_CLOUD_NAME`/`VITE_CLOUDINARY_UPLOAD_PRESET` (all real, used-with-fallback vars), backend was missing `BASE_URL` — added all with explanatory comments so a fresh clone has a complete, accurate `.env.example`. Re-verified `tsc -b`/`eslint`/`vite build` clean on both sides after both fixes. No other misconfiguration found: query-invalidation coverage, Sentry tunnel, layout alignment, and Redis-is-intentionally-not-implemented all re-confirmed consistent with what was actually shipped in REQ-1626–1629. |
| REQ-1631 | Improve SEO metadata in `index.html` + `public/manifest.json` (+ robots/sitemap): title, description, author (Arnob Mahmud / https://www.arnobmahmud.com / contact@arnobmahmud.com), keywords, Open Graph, Twitter, icons, canonical. Keep existing assets; refresh stale Netlify/Yahoo author URLs. Demo URL from README: `https://codebook-aws.vercel.app/` (legacy AWS demo — new Express `backend/` not cut over on Vercel yet). | DONE (2026-08-01) | R0 | User ask 2026-08-01. |
| REQ-1632 | Ensure `lint` scripts exist and pass for frontend + backend; configure ESLint cleanly without breaking established patterns (context+hook colocation, TanStack Table). | DONE (2026-08-01) | R0 | Frontend already had `lint`; backend added. |
| REQ-1633 | Rewrite educational root `README.md` for the Vite + Express stack (features, structure, env, APIs, reuse, keywords) + add root `SECURITY.md` (private reports → contact@arnobmahmud.com). Document migration caveat: new `backend/` not deployed; Vercel not cut over; live demo remains legacy AWS showcase. | DONE (2026-08-01) | R0 | User ask 2026-08-01. |
| REQ-1634 | Enrich catalog UI using the user's separate `university-library` reference project as design/data inspiration: stylized "3D book" cover art (color + spine SVG) on list cards, detail page, and admin CRUD; optional trailer video on detail page; "You Might Also Like" recommendations section on product detail. | DONE (2026-08-01, browser-verified) | R1 | Added `coverColor`/`videoUrl` (both nullable) to `Product` schema via `prisma db push` (project uses db push, not migrate — confirmed via `prisma migrate status`, no migrations folder exists). New shared components: `BookCover` (memoized SVG cover, ported from reference project, graceful fallback to flat image when `coverColor` unset), `ProductVideo` (native `<video>`, renders nothing without a URL), `ProductReel` (see REQ-1635 for full rewrite). New `useRecommendedProducts` hook — scored by shared tags/category, derived from the already-cached `useProducts("")` list, zero extra network call. Wired into `ProductCard`, `ProductDetail`, `AdminProductDetailPage`, admin products table thumbnail, and admin `ProductForm` (new Cover & Media fields). Seed data (`data/db.json`) backfilled with `coverColor`/`videoUrl` reused from the reference project's own seed (same author's ImageKit account). Also extended `adminService.getAdminStats()` (all client-derived from the existing products cache, no new endpoint) with category/language/publication-year breakdowns, top-rated products, and catalog-health metrics — new `AdminCatalogInsights` component on the admin dashboard, styled to match existing Card/progress-bar conventions (not the reference project's DaisyUI classes). `package.json` bumped to 0.2.4 (AdminStats/Product query-shape change). Verified live in-browser: cover art rendering, video playback, reel pause/resume, and an actual admin edit propagating instantly to admin table + storefront list + detail page via client-side navigation (TanStack Query invalidation, zero page reloads). |
| REQ-1635 | Follow-up refinement on REQ-1634 per user feedback: (1) merge all 17 books from the reference project's `dummybooks.json` as new catalog products (not just recolor existing ones) with cover images downloaded locally instead of hotlinked; (2) fix `coverColor` values too close to white, which hid `BookCoverSvg`'s decorative white spine-curl lines; (3) nicer card/detail cover presentation; (4) replace the reel's discrete "jump" auto-scroll with a smooth, continuous, infinitely-looping ticker-tape motion. | DONE (2026-08-01, browser-verified) | R1 | Downloaded all 17 cover images (confirmed `image/jpeg` via HEAD request despite `.png`-looking URLs) into `public/images/10016.jpg`–`10032.jpg`. One-off Node script (run from scratchpad, not committed) merged the 17 books into `data/db.json` with every CodeBook-specific field synthesized (price tiered by page count, stock/`in_stock` from the source's own `totalCopies`/`availableCopies`, SKU continuing the existing numbering, tags from genre, category = genre reused directly since it's a free-text field not an enum) — catalog grew from 15 to 32 products, re-seeded successfully. Fixed white-ish covers via a perceived-luminance check (threshold 230/255): caught the 2 literal `#ffffff` + 1 pale `#fff9bf` already live in the 15 existing products (user-visible in their screenshot) plus the same 3 recurring in the new 17, substituted from a curated dark/medium palette — zero white-ish covers remain, verified via API. `BookCover` display polish: shelf-style gradient background + drop-shadow under the book + hover lift on `ProductCard`, larger presentation on `ProductDetail` and `AdminProductDetailPage`. `ProductReel` rewritten from a `setInterval` + `scrollBy` (discrete jump) design to a single `requestAnimationFrame` loop writing `transform: translate3d()` directly to a track containing the product list duplicated once (second copy `aria-hidden`) — wraps seamlessly at exactly one copy's measured width, reads as an infinite TV-ticker loop. Pause-on-hover/resume and manual chevron nudges now adjust the same offset the auto-drift uses, so they never fight; verified via direct transform sampling (smooth ~32px/s drift, exact-pixel nudge on click, hold-still while paused). `tsc -b`/`eslint`/`vite build` clean both sides after every change; `npm audit`/`depcheck` re-confirmed no new issues (both pre-existing accepted risks unchanged: react-router pin, postcss/tailwindcss/autoprefixer depcheck false positives). |
| REQ-1636 | Second follow-up per user feedback: (1) remove the original 15 CodeBook products from the seed, keep only the 17 merged library books; (2) admin `ProductForm`'s cover editor needed the reference project's richer edit experience — a live preview, not just raw inputs; (3) the customer-facing `ProductDetail` page was "so ugly", complete redesign requested. | DONE (2026-08-01, browser-verified) | R1 | Checked DB dependencies before deleting: `Review.productId`/order `cartList` items have no FK constraint (plain strings/JSON snapshot), so deletion was safe — found and deleted 1 orphaned review referencing a to-be-removed product, orders keep their own snapshot data so they're unaffected. Trimmed `data/db.json` to the 17 merged products, deleted the 15 old rows + the orphaned review from the dev DB, deleted the now-fully-unused `public/images/10001–10015` (confirmed zero references anywhere outside `db.json` first). Caught and fixed a real gap this deletion introduced: zero products were marked `featured_product`, which would've left the homepage's "Featured eBooks" section rendering an empty grid under its heading — marked 3 (Algorithms, Atomic Habits, Cracking the Coding Interview) featured. Admin `ProductForm`: added a live `BookCover` preview next to the color/video inputs, reacting in real time to the color picker/hex input and the product image already uploaded, plus a "Clear" button and an inline reminder not to use white/near-white; verified reactivity live (color change instantly redraws the preview) without submitting (test didn't persist). `ProductDetail` completely redesigned: replaced the old single heavy bordered card containing a title/overview banner plus a wall of 6+ colored info pills with a cleaner hierarchy — category eyebrow, title, author, rating, one-line stock status (previously duplicated pill logic), prominent price + Add to Cart, a scannable two-column "Book Details" info grid (icons + label + value rows) replacing the pill wall, the QR code demoted to a collapsible `<details>` utility (previously competed visually with the buy button), and Description moved to its own full-width section below the hero instead of being buried in the same crowded column. All existing logic (cart add/remove, review CRUD, recommendations reel, video, invalidation) preserved untouched — only the JSX/layout changed. `tsc -b`/`eslint`/`vite build` clean both sides; verified live in-browser (new layout screenshot, admin cover-preview reactivity). |

| REQ-1637 | Fix a real payment-tampering vulnerability found during a routine audit (user confirmed: "fix... make it all secured... real world professional way"): `POST /payment/create-intent` trusted a client-supplied charge `amount`, and both that route's and `POST /orders`'s Stripe metadata/body handling let a client-controlled field override the JWT-verified user identity. | DONE (2026-08-01, verified against a real Stripe test-mode charge) | R2 | Two independent tampering vectors, both closed: (1) `payment.routes.ts` — the client used to send a pre-computed `amount` in cents straight into `stripe.paymentIntents.create()`; now it sends only `cartList: {id, quantity}[]`, the server bulk-looks-up live prices (new `products.service.getProductsByIds`) and recomputes the charge total itself, rejecting the request if any product no longer exists or the total is below Stripe's $0.50 minimum. Also stopped spreading client-sent `metadata` into the PaymentIntent at all — `userId`/`userEmail`/`userName` now come only from `req.user` (the JWT payload), since the old code spread client metadata *after* setting those fields, letting a request body silently override whose account a webhook-fallback order would be attributed to. (2) `orders.routes.ts`'s `POST /orders` — `amount_paid` was taken verbatim from the request body even though the real charge had already happened; now, whenever `paymentIntentId` is present, the route retrieves the actual PaymentIntent from Stripe, confirms it belongs to `req.user` and has `status: "succeeded"`, and overwrites `amount_paid` with the verified `paymentIntent.amount / 100` — so the stored order record (and admin revenue analytics) can never diverge from what Stripe actually charged. Frontend simplified to match: `paymentService.createPaymentIntent(cartList)` (dropped the now-unused `amount`/`user` params — pure redundancy once the backend derives identity from the token), `usePayment.ts` and `StripeCheckout.tsx` updated accordingly. `tsc -b`/`eslint --max-warnings=0`/`vite build` clean both sides. Verified against a real Stripe test-mode charge end-to-end: added a $9 product to cart, confirmed `create-intent` recomputed $9.00 server-side, completed payment with Stripe's `4242...` test card, confirmed the DB order row's `amount_paid` (9) exactly matched the retrieved PaymentIntent's actual charged amount, confirmed the cart badge updated 1→0 instantly with zero page refresh; test order/stock rolled back afterward so no synthetic data was left in the demo DB. |
| REQ-1638 | KPI/product card equal-height + description-clamp consistency (user: "display dynamic card, row having same height... in reel in other places also... display with truncated description"). | DONE (2026-08-01, verified) | R0 | `AdminMetricsCard`/`ProductCard`/`ProductCardSkeleton` given `h-full flex flex-col` (+ shared `min-h` on the KPI card) so grid rows stay equal-height regardless of subtitle/breakdown presence; `ProductCard` overview gained `line-clamp-3` (matches skeleton's 3-line placeholder). `ProductReel` inherits the fix for free since it reuses `ProductCard`. |
| REQ-1639 | Auto-refund a paid order on cancel (user: "on cancel the order as its paid it should refund as well and also restore product stock quantity" — stock restore already existed, refund didn't). | DONE (2026-08-01, verified against a real Stripe test-mode refund) | R2 | `PUT /admin/orders/:id/status` detects `status === "cancelled"` on a `paymentStatus === "paid"` order and reuses the Stripe refund flow (extracted into a shared `refundOrderPayment()` helper, also used by the pre-existing dedicated `/refund` route) instead of a bare status update. Found in the process: the admin status dropdown's "Refunded" option was never backend-valid — removed from the editable select (kept in the filter dropdown, which is a real filterable state), refunded orders now render a plain badge instead of a `<select>` silently defaulting to "Pending" (an unmatched `value` on a native select falls back to the first `<option>`). Verified live: paid $9 order → cancel → real Stripe refund (`re_...`) issued, stock restored to original, UI correctly shows "Refunded" + refund ID/amount, no page refresh. |
| REQ-1640 | On-demand invoice download (user suggestion, accepted): `invoice.service.ts` (REQ-1612) only ever attached the PDF to the confirmation email, no download-anytime option. | DONE (2026-08-01, verified) | R1 | New `GET /orders/:id/invoice` (ownership-checked) streams the existing `generateInvoicePdf()` output. Shared `downloadOrderInvoice()` triggers a real browser download (blob + object URL), wired to a "Download Invoice" button on both the customer and admin order detail pages. Verified: downloaded and confirmed a valid 1-page PDF via the admin UI. |
| REQ-1641 | Idempotency on order creation (user suggestion, accepted): a double-click/retried request after a network blip could create two DB orders for one Stripe payment. | DONE (2026-08-01, verified with a real duplicate-request test) | R2 | Added a unique constraint on `Order.paymentIntentId` (`prisma db push`, checked first for existing duplicates — none). `createOrder()` checks for an existing order by `paymentIntentId` before touching stock (avoids a wasted decrement+rollback in the common retried-request case), plus a `P2002` race-condition safety net returning the winning concurrent request's order. Verified: fired the identical create-order request twice against one real succeeded PaymentIntent — both responses were the same order id/timestamp, exactly 1 DB row, stock decremented exactly once (500→499, not 498). |
| REQ-1642 | Rate limiting on payment/order endpoints (user suggestion, accepted): only auth endpoints had `express-rate-limit` (REQ-1624). | DONE (2026-08-01, verified) | R1 | New `paymentLimiter` (30 req/15min, same pattern as `authLimiter`) applied to `POST /payment/create-intent` and `POST /orders`. |
| REQ-1643 | Refund/cancel reason capture in the admin UI (user suggestion, accepted) — implemented together with REQ-1639 (same dialogs). | DONE (2026-08-01, verified) | R0 | Optional free-text reason `FormTextarea` added to both the Cancel and Refund confirm dialogs (`AdminOrderDetailPage` and `AdminOrdersPage`'s inline cancel dialog). Captured only as an activity-log audit note — never forwarded to Stripe's own 3-value `reason` enum (`duplicate`/`fraudulent`/`requested_by_customer`), which the pre-existing refund route was passing an unchecked free-text value into (`toStripeRefundReason()` now whitelists it, a latent bug fixed in passing). |
| REQ-1644 | Product detail (admin) analytics (user suggestion, accepted): sales-over-time, units sold, revenue, refund/cancellation rate for that product. | DONE (2026-08-01, verified) | R0 | `calculateSingleProductAnalytics()` extended with a monthly sales-trend array (rendered via new reusable `Sparkline` UI primitive — plain SVG, no chart library) and refund/cancellation rate, alongside the existing purchase-count/revenue/AOV stats already on `AdminProductDetailPage`. "View count" (page-view tracking) deliberately not added — would need new write-on-every-view infrastructure, disproportionate to this pass. |
| REQ-1645 | User detail (admin) analytics (user suggestion, accepted): lifetime value, order status breakdown, last-order date, refund/cancellation history. | DONE (2026-08-01, verified) | R0 | New `calculateCustomerInsights()` — all derived from the customer's own already-fetched `orders` array, no new endpoint — shown in a new "Customer Insights" card on `AdminUserDetailPage`. |
| REQ-1646 | Order detail timeline enrichment (user suggestion, accepted): extend to show refund/tracking events inline, not just status changes. | DONE (2026-08-01, verified) | R0 | Found the admin order detail page had **no timeline at all** — only the customer-facing `GET /orders/:id` included `getOrderActivityTimeline()`. Added it to the admin route too, and extracted the customer page's existing timeline rendering (which already handled refund/tracking events inline) into a new shared `OrderTimeline` component so both pages stay identical. |
| REQ-1647 | Order-status KPI breakdown badges (user suggestion, accepted), matching the stock-inventory reference project's in/low/out badge pattern. | DONE (2026-08-01, verified) | R0 | Already existed on the admin Dashboard's Total Orders KPI; found the Analytics page's equivalent KPI was missing it. Added `ordersByStatus` to `AnalyticsSummary`/`calculateAnalyticsSummary()`, wired into `AdminAnalyticsPage`. |
| REQ-1648 | AI restock prediction (user suggestion, accepted). | DONE (2026-08-01, verified) | R0 | Implemented as **deterministic math, not an LLM call** — a numeric days-until-stockout projection from 30-day order velocity is more reliable/explainable as plain arithmetic than a model guess. New "Restock Forecast" card (`AdminCatalogInsights`), linking to the product. |
| REQ-1649 | AI sales trend narrative (user suggestion, accepted). | DONE (2026-08-01, verified — no new code) | R0 | Already fully covered by the existing "AI Business Insights" panel (REQ-1613), which already turns revenue/orders/top-sellers/user-growth into a plain-English recommendation via the multi-provider LLM chain. No duplicate feature built. |
| REQ-1650 | AI order fraud/anomaly scoring (user suggestion, accepted). | DONE (2026-08-01, verified) | R1 | **Deterministic**, not an LLM call — new `calculateOrderRiskFlags()` flags an order at 3×+ a customer's own historical average (or 3×+ the store average for a first-time customer), shown as a "⚠ Review" badge on `AdminOrdersPage` — review signal only, never an auto-block. |
| REQ-1651 | AI review sentiment / fake-review flag (user suggestion, accepted). | DONE (2026-08-01, verified against real multi-provider LLM output) | R1 | The one item that genuinely needed a language model. New `analyzeReviewSentiment()` (reuses REQ-1613's multi-provider chain) runs **on-demand per admin click** (`POST /admin/reviews/:id/analyze-sentiment`, new "Analyze with AI" button on `AdminReviewDetailPage`), not automatically on every review — keeps LLM cost/latency opt-in, off the checkout-adjacent review-creation path. Verified against real provider output (Gemini) for both a genuine positive review and a low-effort one-word review. |
| REQ-1652 | AI dynamic/suggested pricing (user suggestion, accepted). | DONE (2026-08-01, verified) | R0 | **Deterministic**, not an LLM call — new `calculateSuggestedPrice()` (30-day sell-through rate) shown as a dismissible banner in admin `ProductForm` with an "Apply to field" button that only copies the value into the price input; admin must still explicitly save (never auto-applied). |
| REQ-1653 | AI churn/repeat-purchase likelihood (user suggestion, accepted). | DONE (2026-08-01, verified) | R0 | **Deterministic**, not an LLM call — `calculateCustomerInsights()` (REQ-1645) also computes a low/medium/high churn heuristic from days-since-last-order vs. that customer's own average reorder interval (`null`/"not enough history" below 2 orders). |
| REQ-1654 | Low-stock/out-of-stock admin digest email (user suggestion, accepted): daily rollup instead of a per-order ping. | DONE (2026-08-01, verified against a real Brevo send) | R0 | New `admin-low-stock-digest` email template + admin-only `POST /admin/notifications/low-stock-digest` (deliberately its own endpoint, not the pre-existing unrestricted `/email/send`). Triggered by a new "Email Stock Digest" button (`AdminCatalogInsights`), **not a cron job** — a scheduler would be new backend infrastructure beyond this pass's scope; the digest itself is fully real (verified via an actual Brevo send: "0 out of stock, 1 low stock") and a scheduled job could call the same route later unchanged. |

| REQ-1655 | Post-implementation deep audit of REQ-1638–1654 (user: "deep audit, analyze, investigate everything properly handled... any gap or improvement"), before user tests/commits. | DONE (2026-08-02, verified) | R1 | Full re-verification, not just re-reading code: `tsc -b`/`tsc --noEmit` + `eslint --max-warnings=0` + `vite build`/backend `build` clean on both sides; confirmed every new/changed mutation hook invalidates the correct Query keys (or is intentionally invalidation-free for ephemeral results, documented inline); confirmed every new/changed route has `requireAuth`/`requireAdmin` + zod (`createOrderSchema`) or manual type-narrowing on all body fields, plus ownership checks (`GET /orders/:id/invoice`); confirmed no `any`, no dead code, no leftover `console.log`/debug artifacts, no duplicate API calls (`ProductForm`'s pricing suggestion reuses the same `["admin-orders"]` cache key as `AdminOrdersPage`, not a second fetch); confirmed old inline order-timeline code was fully removed (not duplicated) after extraction into `OrderTimeline`. One real gap re-surfaced and fixed: `POST /email/send` (pre-existing, flagged but not fixed in REQ-1654) had no restriction on the `to` address — any authenticated non-admin user could email any template to any address (open relay). Fixed by restricting non-admin callers to `to === req.user.email` or the fixed `ADMIN_ALERT_EMAIL`, preserving every legitimate self-service flow (order confirmation/shipping/refund emails to self, admin alert pings) while closing the arbitrary-recipient gap; admins remain exempt (legitimate on-behalf-of-customer sends, e.g. resending an invoice). |

**Design note across REQ-1648/1650/1652/1653:** all four were originally framed as "AI-driven" but implemented as deterministic math (order velocity, sell-through rate, historical averages) rather than LLM calls — a numeric projection or threshold comparison is more reliable, instant, and free to run than asking a model to guess at arithmetic. The LLM is reserved for genuinely language-shaped problems (REQ-1613's narrative summary, REQ-1651's sentiment judgment). No new npm dependencies were added anywhere in REQ-1638–1654 — every route/component/hook reuses infrastructure already in the codebase.

---

## Traceability Note

This baseline was reconstructed from the current codebase (file inventory), not from a prior `.agile-v/` state — none existed at bootstrap time (see `DECISION_LOG.md` DEC-0001). Baseline REQs are marked `BASELINE` rather than `DONE`: they describe shipped behavior inferred from code presence, not independently verified test evidence. Recommend routing any REQ through Red Team Verifier the next time its code is touched, rather than a big-bang audit.
