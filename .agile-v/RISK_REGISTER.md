# RISK_REGISTER.md

Format: `RISK-ID | Description | Likelihood | Impact | Linked REQ | Mitigation | Owner | Status`

| RISK-ID | Description | Likelihood | Impact | Linked REQ | Mitigation | Owner | Status |
|---|---|---|---|---|---|---|---|
| RISK-0001 | Uncommitted 273-file repo restructure could be lost or accidentally discarded by a future destructive git command before it's committed. | Medium | High | REQ-1101, REQ-1102, REQ-1103 | Flagged as CHECKPOINTS.md CKPT-0001; never run `git checkout .` / `reset --hard` / `clean -f` until resolved; user confirmation required. | Arnob Mahmud | OPEN |
| RISK-0002 | Baseline REQs (0001-1000) are marked `BASELINE` from code inventory only, not independently re-verified — a latent defect in, e.g., payment/refund logic could be reported as "already working" when it isn't. | Low | High (for R3 domains: auth, payment, refunds) | REQ-0100, REQ-0101, REQ-0301-0304, REQ-0406, REQ-0802 | Red Team Verifier pass required before/whenever these REQs' code is next touched (POLICY.yaml gate2_requirements R3). | Red Team Verifier | OPEN — mitigated by policy, not yet executed |
| RISK-0003 | `@aws-sdk/client-dynamodb` lives in root `dependencies` (not devDependencies) alongside the CRA frontend — if any `src/` code imported it directly instead of going through the Lambda API, AWS config could leak into the browser bundle. | Low | Critical | REQ-0004 | Verified this session: `grep -rl "@aws-sdk" src/` returns no matches; usage is confined to `lib/` and `aws-lambda/` (server-side only). Re-check at any future touch of `src/services/*`. | build-agent-js / red-team-verifier | MITIGATED — verified 2026-07-30, no action needed unless a future change adds a `src/` import |
| RISK-0005 | AWS account (849695476938) closes 2026-08-16 (free-tier period ended). Production backend (AWS Lambda + API Gateway) becomes unreachable and all DynamoDB data is permanently deleted after that date. | High (deadline is fixed) | ~~Critical~~ Accepted — no mitigation needed | REQ-1200 to REQ-1213 | **Downgraded 2026-07-30**: user explicitly does not need the live DynamoDB data preserved (REQ-1203 → WONT-DO). New backend will be freshly seeded from `data/db.json` + new test accounts (REQ-1204) instead of migrated production data. Outage after 2026-08-16 is expected and accepted as long as the new backend ships before/around that date. | Arnob Mahmud | CLOSED — accepted by decision, not mitigated |
| RISK-0007 | Google OAuth Client Secret (`GOOGLE_SECRET`) was shared in plain text in chat (2026-07-30) for REQ-1502. If committed to any tracked file (docs, `.env.example`, source), it becomes part of git history and is compromised. | Low (if handled correctly) | High if leaked | REQ-1504 | Never write the secret value into any file this agent creates or edits. Store only in gitignored `.env`/`.env.local` locally and Coolify environment variables in production. If it is ever accidentally committed, rotate it immediately in Google Cloud Console. | agile-v-core / user | OPEN — mitigation is procedural discipline, enforced every time REQ-1502/1504 are touched |
| RISK-0006 | Combining the AWS→Express/Postgres migration and the JS→TypeScript conversion into one effort under the Aug 16 deadline risks rushing both and breaking production. | Medium | High | REQ-1200 to REQ-1305 | Explicit phase separation: backend platform migration (REQ-1200s) ships first under the deadline; TypeScript conversion (REQ-1300s) follows afterward with no deadline pressure, once the new backend is stable. | agile-v-core / user | OPEN — sequencing decision recorded in DECISION_LOG.md |
| RISK-0004 | `docs/` folder contains several generic cross-project reference guides (VPS migration, Coolify, Redis/Sentry/PostHog, LLM fallback) that reference *other* projects (e.g. "FreeScribe", "hotel-booking-backend") — risk of confusing future agents about this project's actual deployed infra (Vercel + AWS Lambda, not VPS/Docker). | Low | Low | REQ-0004, REQ-0005 | PLAYBOOK.md explicitly states actual stack; treat `docs/*_GUIDE.md` as reusable templates, not this-project infra truth, unless a REQ says otherwise. | agile-v-core | OPEN — informational |
