# STATE.md — Single Source of Truth for "where are we right now"

**Project:** CodeBook E-Commerce Platform
**Cycle:** C1 (brownfield-baseline)
**Updated:** 2026-08-01T13:40:00Z
**HEAD:** `949d846` (`main` == `origin/main`)

## Current Position on the V

```text
Specify ──── Constrain ──── Orchestrate (apex) ──── Prove ──── Verify
                                    ▲
   HERE — Docs/SEO/lint/env audit pass complete. Pipeline idle for next intent.
   Coolify deploy deferred. Gate 2 / Red Team optional.
```

- **Stage:** Pipeline **idle** after Orchestrate/Prove — next entry is Stage 1 (Specify) on new intent, or Stage 4 (Verify) if user asks for Red Team / Gate 2
- **Pipeline status:** Infinity Loop **ACTIVE**; governance synced 2026-08-01
- **Human Gate:** None PENDING. CKPT-0001 **RESOLVED + COMMITTED** (`14e53b2`)

## Session activation (2026-08-01) — resume, not fresh bootstrap

1. Loaded `agile-v-core` + `agile-v-pipeline` (+ lifecycle/compliance on demand). Confirmed `.agile-v/` already fully populated from prior C1 work (not empty).
2. Synced stale governance to live stack: Vite SPA + Express/Prisma (config/PLAYBOOK/POLICY no longer claim CRA/Lambda as primary).
3. Closed REQ-1101/1102/1103 → **DONE** (commit `14e53b2` already on `origin/main`).
4. Bootstrapped missing Infinity Loop hooks: `AGENTS.md`, `.agile-v/ACTIVATION.md`, `.agile-v/SKILLS.md`, `.agile-v/skills/` (24 + index), `.cursor/rules/agile-v-infinity-loop.mdc`.
5. **No application code** written this turn — activation/sync only.

## Resolved Checkpoint

**CKPT-0001** — Initially (2026-07-30): leave restructure uncommitted. Later (2026-08-01): user authorized commit/push → included in `14e53b2`. REQ-1101/1102/1103 **DONE**.

## Phase 1 Build Status (2026-07-30) — Orchestrate/Prove in progress

Backend (`backend/` — Express + Prisma) and the Google sign-in / avatar frontend pieces are **built and locally verified** (real Postgres 14, live HTTP smoke tests — see DECISION_LOG 00:00:14Z). Production frontend build compiles clean.

**Full-codebase audit pass completed (2026-07-30, see DECISION_LOG 00:00:15Z):** REQ-1213 (root `lib/` + obsolete DynamoDB/`vercel dev` scripts removed), REQ-1214 (ESLint suppression removed + all 20 real findings fixed, dead migration-era code removed, debug `console.log` traces stripped from 7 files), REQ-0007 (global ErrorBoundary added — was a real gap), REQ-1508 (Google OAuth CSRF `state` validation was claimed but not implemented — now fixed and live-tested), REQ-1401 partial audit (found + fixed a real `admin-reviews` invalidation gap in `useReviews.js`).

**Second audit pass + full local integration test completed (2026-07-30, see DECISION_LOG 00:00:16Z):** REQ-1401 audit finished (all `useMutation` sites in `src/` now checked). REQ-0008 (real `Array.sort()` in-place-mutation bug in `FilterContext` fixed). REQ-0306 (cart had zero persistence — a refresh silently emptied it; now persisted to `localStorage`). REQ-0009 (dead `LoadingContext`/`LoadingProvider`, zero consumers, removed). REQ-1409: stood up the full local stack (frontend on :3000, `backend/` on :4000, persistent local Postgres `codebook_dev`, real Stripe/Brevo/Shippo test keys) and drove the whole app through a real browser via Playwright — login (both roles), RBAC, full Stripe test-card checkout → real order, instant cache invalidation across cart/dashboard/admin-orders/admin-products/reviews, stock decrement, `UserAvatar` rendering. Zero app-level console errors. **The app is functionally working end-to-end on localhost with the new backend right now** — `npx react-scripts start` (frontend, :3000) and `node backend/src/server.js` (backend, :4000) are both running in the background from this session; root `.env.local` (gitignored) is what points the frontend at the local backend instead of AWS.

**Third pass — DB-driven demo login, content enrichment, Sonner/AlertDialog, ticket/notification test (2026-07-30, see DECISION_LOG 00:00:17Z):**
- REQ-0104: demo-account login is now fully database-driven (`User.isDemo`, `GET /auth/demo-accounts`, `POST /auth/demo-login`) — no credentials of any kind live in the frontend bundle/env anymore; one-click login instead of fill-then-submit. Also fixed a real bug where a manually-logged-in admin's role got silently overwritten to `"user"`.
- REQ-0204: `Product` schema extended with `author`/`category`/`level`/`pages`; all 15 seed products rewritten with unique professional copy (zero Lorem ipsum left anywhere in `src/`); `prisma/seed.js` now actually syncs `data/db.json` content on re-run instead of no-op'ing on existing rows.
- REQ-1509: real Google OAuth credentials wired into `backend/.env`; redirect confirmed hitting real Google with the real client_id. The interactive consent-screen login itself was **not** attempted (an automated agent can't/shouldn't do a real Google login) — needs the user to click through once, and `GOOGLE_CALLBACK_URL` needs to be a registered Authorized redirect URI in Google Cloud Console.
- Full ticket create → admin reply → status auto-transition → customer notification cycle verified live via Playwright, no manual refresh anywhere.
- REQ-1600/1601: `react-toastify` fully replaced with shadcn/ui Sonner (dynamic per-message title derivation); fixed `authService.js` swallowing real backend error messages.
- REQ-1602: order-cancellation now requires `AlertDialog` confirmation (was firing instantly from a plain select) on both the orders list and order detail admin pages — product/user delete and refund already had this.
- REQ-1603/1604: homepage testimonials and FAQ rewritten (were literal unmodified Flowbite template text about Figma/Tailwind and 100% Lorem ipsum respectively).
- REQ-1605 (Lucide icon pass across every title/label/button/filter): **deferred, not started** — explicitly scoped out this round rather than rushed, see REQUIREMENTS.md for reasoning. Flagged for the user to direct explicitly.
- Frontend production build clean throughout (final: 305.34 kB gzip JS, 0 errors/warnings). Both dev servers (frontend :3000, backend :4000) still running from this session against local Postgres `codebook_dev`.

**Fourth pass — animation system, RippleButton, KPI card restyle, hero background, clickable links (2026-07-30, see DECISION_LOG 00:00:18Z):**
- REQ-1605 (icon pass): partial. `lucide-react` installed and used in the new KPI cards; a full sweep of every remaining `bi-*` site was not attempted (avoid mixing two icon systems under time pressure — see REQUIREMENTS.md notes). Still needs explicit scope direction.
- REQ-1606: `framer-motion` + a shared `ScrollReveal`/`StaggerContainer` motion module (`src/components/ui/motion.js`); staggered field-by-field reveal on Login/Register, per-route fade+slide on every navigation (`App.js`). **Real bug found via screenshot, not just a clean build**: an async-gated `StaggerItem` (Login's demo-account dropdown) never became visible because Framer Motion doesn't replay an already-fired parent transition for children added later — fixed by using an independent `ScrollReveal` for that block instead.
- REQ-1607: `RippleButton` component built and applied to the Login/Register submit buttons; not yet rolled out to every button in the app (ready for that as a follow-up).
- REQ-1608: `AdminMetricsCard` rewritten with gradient/colored-border/icon-badge/breakdown-pill styling matching `docs/UI_STYLING_GUIDE.md`'s card system (adapted to this app's light-first theme) and the user's `stock-inventory` screenshots. Applied to Dashboard + Analytics summary rows; deeper Analytics sub-sections not converted.
- REQ-1609: rotating Ken-Burns hero background, React-ported from the vanilla-JS spec, dropped into the existing two-column Hero's image slot (no layout redesign).
- REQ-1610: audited every admin list page for plain-text product/order/user references — found and fixed 3 real gaps (Orders: Order ID + Customer were plain text; Products: name was plain text; Reviews: showed a raw unlinked UUID with zero product-name lookup). All now sky-colored links to the right detail page. `AdminUsersPage.js` already worked. Tickets/History/customer-facing Dashboard order list not yet audited.
- Explicitly NOT started this round (see REQUIREMENTS.md REQ-1611–1615 for full reasoning): TanStack Table migration across admin list pages (library installed, migration itself is a separate sizeable initiative), auto-invoice PDF generation+email, multi-model AI insights (blocked on the user's promised API key), `SAFE_IMAGE_REUSABLE_COMPONENT.md` (N/A — Next.js-only pattern, this is CRA), `VERCEL_PRODUCTION_GUARDRAILS.md` (Next.js-specific + not deployed yet, low priority).
- `docs/PROJECT_PLAN.md` §11 now has the same done/deferred breakdown for anyone reading that doc instead of this one.
- Frontend production build clean throughout (final: 352.21 kB gzip JS). Every visual change checked with an actual Playwright screenshot or live click-through.

**Fifth pass — documentation + Phase 3 cache-invalidation audit (2026-07-31, see DECISION_LOG 00:00:19Z):**
- `docs/PROJECT_PLAN.md` §6 rewritten with a per-item ✅/🚧/⬜/N/A checklist across all four phases (was prose-only before); a Phase 4 (UI/UX polish, REQ-1600–1615) section added to match what actually shipped in the third/fourth passes; top-of-file status line updated to stop claiming "Planning, no code written yet" (stale since Phase 1 was built).
- `docs/PROJECT_WALKTHROUGH.md` written from empty — repo layout, local run instructions, data model, auth (incl. the DB-driven demo-login flow), frontend/backend architecture patterns, animation-system gotcha (the `StaggerItem`-vs-`ScrollReveal` bug from the fourth pass), for onboarding without reading this whole file.
- Real dead-code find: `src/services/apiClient.js` (328 lines) — a pre-migration, AWS-Lambda-URL-pointing API client, fully unreferenced anywhere in `src/` (confirmed via repo-wide grep) and untracked in git. Deleted; fixed the one remaining stray comment reference to it in `backend/src/routes/products.routes.js`. This cleared both pre-existing frontend ESLint warnings (`no-unused-vars` on `getUserId`, `import/no-anonymous-default-export`) — `npx eslint src` is now 0 errors/0 warnings.
- REQ-1401 (cache-invalidation audit) actually performed, not just claimed: read every mutation hook (`useAdmin.js` — 10 mutations, `useReviews.js`, `useTickets.js`, `useNotifications.js`, `usePayment.js`, `useImageUpload.js`) plus `src/utils/queryInvalidation.js` and cross-checked every `invalidateQueries` call's key against the actual `queryKey`s used by `useProducts.js`/`useUser.js`/`useAdmin.js` queries. Finding: coverage is already comprehensive and correct — every admin CRUD mutation invalidates the current entity, its list, `admin-stats`, and `admin-activity-logs`; order-affecting mutations invalidate both `admin-orders` and the specific customer's `user-orders`; stock-affecting mutations (order cancel/refund, product CRUD) invalidate product queries too. No missing-invalidation bugs found this pass.
- Frontend production build re-verified clean after the deletion (352.21 kB gzip JS, 0 errors/warnings); backend re-verified with `node --check` on every file (`BACKEND OK`).
- Explicitly not touched this pass (still open, no new scope decided): REQ-1605/1607/1608/1611–1615 — unchanged from the fourth pass, still need user direction on scope.

**Sixth pass — Phase 2 TypeScript + Vite migration (2026-07-31, see DECISION_LOG 00:00:20Z):**
- User explicitly pulled Phase 2 forward this round ("first migrate this - Phase 2... and Phase 3"), superseding the original plan's "starts only after Phase 1 is stable in production" gate. Sequenced backend-first (self-contained, lower blast radius) then frontend, verifying the build after each stage rather than converting everything before testing anything.
- **Backend (100% done, verified):** all 26 files under `backend/src/` (`lib/`, `services/`, `routes/`, `app.ts`, `server.ts`) converted `.js` → `.ts`, plus `prisma/seed.ts`. `tsconfig.json` strict (`noUncheckedIndexedAccess`, `noImplicitOverride`, `noUnusedLocals/Parameters`, `noFallthroughCasesInSwitch`). Added `zod` schemas colocated in each `*.service.ts` (`createProductSchema`, `createOrderSchema`, `registerSchema`, `createReviewSchema`, `createTicketSchema`, `updateUserSchema`, etc.), validated at the route boundary before the service layer runs. Pinned `@types/express@^4` (latest `@types/express` targets Express 5, this app runs Express 4 — would have caused type mismatches). Fixed `moduleResolution: "node10"` (removed in the installed TS version) → `"node16"`. Deleted all 26 superseded `.js` originals + old `prisma/seed.js` only after `tsc --noEmit` passed clean and `npm run build && npm run start` was smoke-tested against the real Postgres DB (`/api/health`, `/products`, `/auth/demo-accounts`, `/login` all returned correct real data).
- **Frontend Vite migration (100% done, verified in a real browser):** installed `vite` + `@vitejs/plugin-react` + `vite-tsconfig-paths` + `typescript-eslint`/`eslint` flat config, uninstalled `react-scripts`. New `vite.config.ts` (port 3000, `build.outDir: "build"` to match existing deploy tooling), root `index.html` (CRA's `public/index.html` deleted, `%PUBLIC_URL%` tokens removed), `tsconfig.json`/`tsconfig.node.json`, `src/vite-env.d.ts` for `import.meta.env` typing. All `REACT_APP_*` env vars renamed to `VITE_*` across `.env`/`.env.example`/`.env.local` and the 4 frontend files that still referenced `process.env.REACT_APP_*` directly. **Real bugs caught and fixed during verification, not just typing changes:**
  - Vite (unlike CRA/webpack) refuses to parse JSX inside plain `.js` files — only `.jsx`/`.tsx`. Bulk-renamed 101 component/page files `.js` → `.jsx` (left the 9 hook files and 7 barrel `index.js` files alone since they contain no JSX).
  - `tailwind.config.js`'s `content` glob was `./src/**/*.{html,js}` — after the rename it would have matched **zero** of the renamed `.jsx`/`.tsx` files, silently shipping an almost-unstyled production build (caught by comparing generated CSS size before/after: 5.12 kB → 61.50 kB once fixed). Fixed to `{html,js,jsx,ts,tsx}` and renamed to `tailwind.config.cjs`.
  - Root `package.json` needed `"type": "module"` (to silence Vite's CJS-config-loader deprecation warning and make `eslint.config.js`'s `import` syntax valid) — which in turn required renaming `postcss.config.js`/`tailwind.config.js` to `.cjs` (they use `module.exports`, which breaks under `"type": "module"` unless the extension forces CJS).
  - `autoprefixer` was only ever present as a transitive dependency of `react-scripts`; removing CRA silently broke Tailwind's PostCSS pipeline until installed directly.
  - `src/index.css`'s `@import` statements came after `@tailwind` — CRA/webpack tolerated this, Vite's stricter PostCSS enforces the CSS-spec rule that `@import` must precede all other statements; reordered.
  - Verified with Playwright in a real running browser (not just curl): homepage, `/products`, and `/admin` (after demo-login as `test@admin.com`) all render correctly with live DB data, correct Tailwind styling, and **0 console errors** on each page.
- **Frontend TypeScript conversion — foundational layer 100% done:** `src/types/index.ts` (new — single-source DTOs mirroring `schema.prisma`, since no shared package exists between frontend/backend), `src/reducers/*.ts`, `src/context/*.tsx`, `src/lib/toast.ts`, `src/utils/*.ts` (7 files), `src/services/*.ts` (13 files, incl. `apiError.ts`), `src/routes/*.tsx`, `src/App.tsx`/`src/index.tsx`, plus `useUser.ts`/`useTitle.ts` and `components/ui/loading-state.tsx`. `tsc -b` passes with 0 errors across the whole strict-typed layer.
- **Deliberately preserved (not "fixed") during conversion — two pre-existing quirks found and left alone since fixing them would be an undisclosed behavior change:** (1) `filterReducer`'s `PRODUCT_LIST` case does `return { productList: payload.products }` without spreading `state`, resetting `onlyInStock`/`bestSellerOnly`/`sortBy`/`ratings` every time the product list reloads — flagged in a code comment, not changed, since it's a UX question for the user, not a typing bug. (2) `products.routes.js`'s `/featured-products` endpoint comment — confirmed (again) that the frontend never actually calls it (derives featured products client-side by filtering the cached product list in `useProducts.js`); comment now says so plainly.
- **Remaining (not yet done):** 9 more hook files (`useAdmin.js` 778 lines, `useAnalytics.js`, `useAuth.js`, `useImageUpload.js`, `useNotifications.js`, `usePayment.js`, `useProducts.js`, `useReviews.js`, `useTickets.js`) and ~101 component/page files are on `.jsx` (Vite-compatible, building and running correctly) but not yet given real prop/return types — they type-check as implicitly-untyped under `allowJs: true` rather than erroring, which is intentional incremental-adoption config, not a gap being hidden. Converting them is mechanical (same patterns as the layer already done) with no remaining architectural decisions.
- `docs/PROJECT_PLAN.md` §6/§11/top-status-line updated to reflect all of the above; this STATE.md entry is the itemized version for anyone picking the remaining `.jsx`→`.tsx` work back up.

## Seventh pass — Phase 2 TypeScript conversion finished 100%, asset restructure, font self-hosting (2026-07-31)

Picked up exactly where the sixth pass left off (9 hooks, 7 barrels, ~110 component/page files still `.jsx`) and finished the conversion completely, plus three items the user requested in the same round that weren't part of the original Phase 2 scope:

- **All 9 remaining hooks converted by hand** (`useAuth.ts`, `useImageUpload.ts`, `usePayment.ts`, `useProducts.ts`, `useNotifications.ts`, `useTickets.ts`, `useAnalytics.ts`, `useReviews.ts`, `useAdmin.ts` — the 778-line one). Caught and fixed two real pre-existing bugs while typing, not just decorating: (1) `useImageUpload`'s `mutationFn` signature took `(file, uploadOptions)` but every real call site does `mutateAsync(file)` — the second param was permanently unreachable dead code, now removed; (2) `useTicket`/`useUpdateReview`/`useUpdateReviewStatus` did a defensive `data.ticket || data` / `data.review || data` unwrap that was already redundant because the underlying service functions unwrap the response themselves — simplified to use the service's return value directly.
- **7 barrel `index.js` files renamed straight to `.ts`** (pure re-export files, no JSX, no logic — safe mechanical rename, no typing work needed).
- **All ~110 remaining component/page files converted**, split across 4 parallel background build-agents (components/ui — 27 files; components/Elements+Layouts+Other+Sections — 22 files; pages/Admin — 19 files; remaining pages — 27 files) plus the orchestrator finishing ~29 files by hand after three of the four agents hit the session's API usage limit mid-task and stopped (their completed work up to that point was intact and merged in; the orchestrator verified `tsc -b` cleanly and finished every remaining file itself rather than re-spawning agents into the same limit).
- **Real bugs surfaced and fixed by strict typing** (not just type decoration) during this pass:
  - `ErrorState`/`EmptyState`/`SearchFilterBar` components had narrower prop interfaces than some call sites actually used (`AdminReviewsPage` passed a nonexistent `filters` JSX prop instead of the component's real `filterValue`/`onFilterChange`/`filterOptions` API; `title` instead of `message`; `StripeCheckout` passed an `action: {label, onClick}` object `ErrorState` didn't support) — fixed the call sites to use the established API, and widened `ErrorState` to support an optional `action` override (falls back to the existing `onRetry`/"Retry" default) since that pattern is legitimate and used in two places.
  - `ReviewForm.tsx`: a validation-error message (`errors.comment`) was being passed to a dead/nonexistent prop and silently never rendered — now renders via the correct `<FormError message={...} />` prop, matching every other form in the app.
  - `AdminOrderDetailPage`'s `canRefund` check had a redundant clause (`paymentStatus !== "refunded"` after already asserting `paymentStatus === "paid"` — TS correctly flagged this as unreachable) — removed the dead redundant condition.
  - `AdminUserDetailPage` read a nonexistent `user.verified` field (always `undefined`/falsy at runtime, so the "Verified" badge always showed "No" regardless of actual status) — fixed to read the real field, `user.emailVerified`.
  - `exportUtils.ts`'s CSV export functions were typed `Array<Record<string, unknown>>`, which TypeScript's structural typing does NOT accept domain interfaces into (missing index signature) even though every property matches — retyped to `Array<object>` with an internal cast, so `RevenueByPeriodEntry[]`/`TopProductEntry[]`/etc. pass through cleanly without a wrapper `as` at every call site.
  - `dataService.ts`'s `createOrder` return type was declared as plain `Order`, but the real backend response (and `PaymentSuccessPage`'s actual logic) includes `_stockUpdates`/`_lowStockAlerts` metadata (`OrderWithStockMeta` on the backend) — added the matching frontend type instead of leaving those fields silently untyped/`any`.
- **React Compiler ESLint rules (`eslint-plugin-react-hooks`'s newer `set-state-in-effect`/`refs`/`static-components`/`preserve-manual-memoization` checks) surfaced 13 real errors across pre-existing code once TypeScript+ESLint actually ran on files that were previously `.jsx` and unchecked.** All fixed, not suppressed wholesale:
  - `HeroRotatingBackground.tsx` mutated a ref during render to precompute the two crossfade layers — rewritten to derive both layer images as plain `const`s from `index`/`active` state on every render (no ref needed at all), which is both a cleaner pattern and satisfies the rule for real, not just quieted.
  - `UserAvatar.tsx` used a `useEffect` to reset a `useFallback` boolean whenever the `image` prop changed — rewritten to track the specific `erroredSrc` value instead of a boolean, so a new `image` prop is trusted again purely from render-time comparison, no effect required.
  - `ProductDetail.tsx`'s `inCart` was mirrored into state via an effect watching `cartList`/`product.id` — replaced with a direct `cartList.some(...)` computed inline on every render; it never needed to be state at all.
  - Three genuinely-necessary "seed editable local state from an async fetch" patterns (`AdminUserEditPage`'s form, `TicketDetailPage`'s status dropdown, `ImageUpload`'s preview) were left as effects with a one-line justified `eslint-disable-next-line` comment each, since the state must diverge from its source after the user starts editing — no effect-free rewrite exists for that case.
  - Two Recharts tooltip components (`sales-trend-chart.tsx`, `top-products-chart.tsx`) defined a `CustomTooltip` component inside the parent component body (recreated every render) — hoisted both `CustomTooltip` and their pure formatter helpers to module scope.
  - Two `useMemo` dependency arrays used optional chaining (`[ticket?.messages]`, `[tickets, user?.email]`) that the compiler flagged as less precise than what it could infer — widened to `[ticket]` / `[tickets, user]` to match.
- **`tsconfig.json`'s `allowJs`/`checkJs` incremental-adoption escape hatch removed** now that 100% of `src/` is `.ts`/`.tsx` (confirmed zero `.js`/`.jsx` files remain anywhere under `src/` with `find`). Also dropped the now-dead `@assets/*` path alias (pointed at the just-deleted `src/assets/`).
- **Asset restructure (user-requested, not originally scoped in Phase 2):** `src/assets/` (bundler-imported `logo.png` + `hero.avif`) deleted; both moved into `public/`. `public/assets/images/*.avif` (15 product images) flattened to `public/images/*.avif`, dropping the redundant `assets/` wrapper. Updated: 3 `import Logo from "../assets/logo.png"` bundler imports → plain `const Logo = "/logo.png"` public-path strings; `data/db.json`'s 20 `image_local` paths `/assets/images/*` → `/images/*`; `DashboardCard.tsx`'s hardcoded fallback path; `ProductForm.tsx`'s placeholder text; `productImage.ts`'s doc comment. Backend reseeded (`npm run seed`, idempotent upsert) so the already-seeded DB rows picked up the corrected image paths too — verified live in the browser afterward (product images and dashboard order-card images render correctly from the new paths). Audited `public/manifest.json`/`logo192.png`/`logo512.png` and confirmed they're actively referenced (`index.html`'s `og:image`/`twitter:image`/`apple-touch-icon`/`<link rel="manifest">`) — kept, not dead PWA scaffolding.
- **Font self-hosting (user-requested):** Poppins was `@import`ed from Google Fonts but never actually applied anywhere in the codebase (`font-family: "Roboto"` is the only font ever set; grepped for `Poppins`/`font-poppins` usage — zero hits outside the import line itself) — removed as dead weight. Roboto is now self-hosted: downloaded the real current variable-font woff2 from Google's static CDN (`fonts.gstatic.com`, resolved via the actual `css2` response rather than guessing a hash filename, which 404'd on the first attempt) into `public/fonts/roboto/roboto-var.woff2` (~43KB, single file, weight range 300–700 covers every `font-light/normal/medium/semibold` utility class actually used in `src/` — grepped and confirmed no `font-bold`/`font-extrabold` usage exists, so 700 was the practical upper bound needed anyway) and added a matching `@font-face` rule; the Google Fonts `@import` is gone entirely, so there's no runtime request to `fonts.googleapis.com`. `bootstrap-icons` (previously the jsdelivr CDN `@import`) is now `npm install`ed and imported directly in `index.css` (`@import "bootstrap-icons/font/bootstrap-icons.css"`), so Vite bundles its font files locally with content-hash cache-busting — confirmed in the production build output (`bootstrap-icons-*.woff2`/`.woff` present in `build/assets/`).
- **Two real mistakes caught and self-corrected mid-pass** (both from a Bash tool `cwd` still pointing at `backend/` from an earlier `cd`, not caught until later verification): the first font-file download attempt landed in `backend/public/fonts/` instead of the frontend's `public/fonts/`; `bootstrap-icons` was initially `npm install`ed into `backend/package.json` instead of the frontend's. Both caught by checking `find`/`git diff` on `backend/package.json` and the actual file locations before finishing, moved/cleaned up, re-verified with a fresh `vite build`.
- **Full verification, this pass:** `npx tsc -b` (frontend) — 0 errors, run repeatedly after every batch of fixes. `cd backend && npx tsc -p tsconfig.json --noEmit` — 0 errors. `npx eslint src` — 0 errors, 6 pre-existing benign `react-refresh/only-export-components` warnings left untouched (established codebase pattern of colocating a Context with its consumer hook, e.g. `CartContext.tsx`, `FilterContext.tsx`, `AdminLayoutContext.tsx`, `motion.tsx`). `npx vite build` — clean, 2931 modules. Real-browser Playwright verification (not just build-log trust) across every major page type while logged in as both demo accounts: home, products list, product detail (with QR code + review form + rating stars), cart, dashboard (both empty-state and populated-with-real-order states), tickets list, and all 8 admin pages (dashboard/products/orders/users/analytics/history/tickets/reviews/settings) — **0 console errors on every single page**, live DB data, correct Tailwind styling, working search/filter bars, working charts with custom tooltips.
- `docs/PROJECT_PLAN.md` §6 Phase 2 rewritten to reflect 100% completion plus two new items (REQ-1306 asset restructure, REQ-1307 font self-hosting); §11 status line updated. `docs/PROJECT_WALKTHROUGH.md`'s repository-layout tree and prose updated — every stale `.js` filename reference (`App.js`, `server.js`, `toast.js`, `motion.js`, etc.) now correctly says `.tsx`/`.ts`.

**Still open before Phase 1 is actually done (deployment intentionally deferred — user wants localhost validated first):**
- REQ-1201/1210: provision `codebook_db` on the real Coolify Postgres container + deploy `backend/` there — not started, by explicit user request ("dont talk about coolify deployment").
- REQ-1211: flip the committed `REACT_APP_LAMBDA_API_URL` in `.env` from AWS to the new backend once it's deployed (currently only the gitignored, local-only `.env.local` points at it, for testing).
- REQ-1212: decommission AWS only after a verified parallel-run.
- REQ-1605, REQ-1611 to REQ-1615: see the fourth-pass bullet above — all need explicit user direction on scope before continuing.
- Independent verification (Red Team Verifier / user) — all testing so far, across all four passes, was Build-Agent self-testing, not independent review, per Directive 4.
- Google OAuth: only the redirect + CSRF check are verified; the actual consent-screen round trip needs the user to click through once themselves.

## Eighth pass — deployment-readiness audit: git tracking, dead code, ESLint scope, persisted cache, admin code-splitting (2026-07-31)

User asked for a fresh deep audit of the whole codebase toward "ready for deployment" (not an actual deploy — Coolify/VPS remains explicitly deferred), plus repeated the standing instant-nav/prefetch/cache request in Next.js SSR terms ("page.tsx" server code, "use client" components). Since this project is a Vite SPA with no server-rendering framework, asked a Human-Decision question first rather than guessing: user chose to stay on the Vite SPA and maximize perceived speed via prefetch/cache/code-splitting rather than migrate to Next.js.

- **Critical finding: git tracking was completely broken.** `git status` showed the entire live codebase (`src/` — 155 files, `backend/src/` — 28 files, `data/`, `docs/`, every root config file) as untracked, while the index still held 258 files under a stale pre-restructure `codebook/` subfolder path and 15 files under a retired reference-backend folder — both physically deleted from disk already. This means a git-based deploy (Vercel, GitHub Actions, etc.) run against the unreconciled repo would have built from missing/stale paths, not the real app — a hard blocker for deployment readiness, independent of and in addition to the standing Coolify/VPS deferral. Verified `.env`/`.env.local`/`backend/.env` are correctly gitignored (`git check-ignore -v`) before reconciling via `git add -A`. **Staged only, not committed** — no commit authorization was given this round.
- **ESLint scope gap:** `eslint.config.js`'s ignore list only covered `dist`/`build`/`node_modules`, so `npx eslint . --ext .ts,.tsx` was also parsing the retired `aws-lambda/` legacy backend and its generated `.aws-sam/` build output, producing 38 spurious parse errors from code that isn't part of the live Express/Vite codebase. Added `aws-lambda` and `**/.aws-sam/**` to the ignore list.
- **Real ESLint error fixed:** `backend/prisma/seed.ts` used a `require()` CommonJS call (with a stale `eslint-disable` comment already not even matching the actual error) to load `data/db.json` inside an async function — replaced with `fs.readFileSync` + `JSON.parse`, which is both lint-clean and more idiomatic for a `.ts` file under `"type": "module"`.
- **Dead code / debug artifacts removed** (grepped every candidate across the repo first to confirm zero live references): `test-migration.js` and `add-vercel-env.sh` at repo root (both hit the retired AWS Lambda API via CRA-era `REACT_APP_*` env vars that no longer exist post-Vite-migration; the `.sh` script also hardcoded a live Stripe publishable key inline), a stray `register-page.png` verification screenshot, and one genuinely orphaned component `src/components/Other/LoadingSpinner.tsx` (zero references anywhere, including barrels). Added `.playwright-mcp/` (this tool's own debug-log/snapshot output directory) to `.gitignore` after finding it had been silently staged.
- **Persisted TanStack Query cache added** (`@tanstack/react-query-persist-client` + `@tanstack/query-sync-storage-persister`), directly addressing the user's repeated explicit ask for localStorage-backed caching so a reload/reopened tab paints from last-known-good data instantly instead of blank-loading. `buster` is tied to `package.json`'s version (injected via a new `vite.config.ts` `define`) so a deploy with a shape-changing query response auto-invalidates old cached data. Considered and rejected a query-key-name-based dehydrate filter for "auth"/"session" data — grepped every `queryKey` in `src/hooks/` and confirmed no such query exists in this codebase (auth lives in `sessionStorage`, not TanStack Query) — relying instead on the three pre-existing logout call sites (`DropdownLoggedIn.tsx`, `AdminHeader.tsx`, `AdminSidebar.tsx`) that already call `queryClient.clear()`, which also wipes the persisted localStorage snapshot. Verified live in the browser: 8 persisted queries dropped to 1 immediately after clicking Logout, confirming no cross-account data leak in a shared browser.
- **Admin console code-split into a lazy chunk.** The production build showed one single 1.2MB/335KB-gzip JS bundle with no splitting. Converted every `admin/*` route in `AllRoutes.tsx` to `React.lazy(() => import("../pages/Admin").then(...))`, importing directly from the `pages/Admin` barrel rather than the mixed root `pages` barrel (a dynamic import of the root barrel would have pulled the whole app, admin included, back into one chunk, since that barrel statically re-exports everything). Removed the admin re-exports from `pages/index.ts` entirely after grepping to confirm `AllRoutes.tsx` was the only consumer, eliminating any bundler ambiguity. Result: a separate `Admin-*.js` chunk (139KB / 28KB gzip) that regular customers — the vast majority of visits — never download. Added an idle-time background prefetch of that chunk in `App.tsx` (`requestIdleCallback`, gated on `sessionStorage.getItem("userRole") === "admin"`) so already-authenticated admins don't pay a chunk-fetch delay on their first `/admin` click. Considered a broader hover-prefetch-on-every-link pattern (product cards, admin table rows) per the user's general "prefetch on click across the codebase" ask, and deliberately did not build it — a large, sprawling change (15+ files) for marginal benefit given the existing 10-minute `staleTime` + the new persisted cache already cover the high-value repeat-navigation case, and the user's own instructions also warn against introducing complicated code logic.
- **Verification, this pass:** `npx tsc -b` (frontend) and `cd backend && npx tsc --noEmit` — both 0 errors. `npx eslint` frontend and backend — both 0 errors (frontend: 6 pre-existing benign `react-refresh/only-export-components` warnings, unchanged from prior passes; backend: 0 warnings). `npm run build` (frontend `vite build` + backend `tsc`) — both clean; frontend build now shows 3 chunks (`Admin-*.js` 139KB, `index-*.js` 307KB, a Rollup-auto-named shared/vendor chunk 770KB) instead of one 1.2MB chunk. Real-browser Playwright verification: logged in as the admin demo account, confirmed the persisted cache populates on load, admin dashboard/analytics(charts)/products all render correctly via the lazy chunk with 0 console errors, and logout correctly clears both the in-memory and persisted query cache.
- Spot-checked mutation cache-invalidation coverage in `useAdmin.ts` (9 mutations, 35 invalidation calls, no mutation found lacking an `onSuccess` invalidation) — consistent with the full audit already completed and documented in an earlier pass; no behavior changed here since hooks were only retyped, not rewritten, in the prior TypeScript-conversion pass.
- `.agile-v/REQUIREMENTS.md` — added REQ-1308 (git tracking reconciliation) through REQ-1311 (admin code-splitting), all DONE (2026-07-31).

**Still open / explicitly not done this pass:**
- The staged git reconciliation (REQ-1308) has **not been committed** — that requires explicit user authorization per this session's standing git-safety rules.
- Coolify/VPS deployment itself remains explicitly deferred by the user; this pass only closed gaps that would have blocked *any* future git-based deploy, it did not deploy anything.
- Hover/render-time prefetch-on-link (beyond the admin-chunk idle prefetch) was evaluated and deliberately not built — see rationale above.
- Independent (non-self) verification / Red Team review — still outstanding across every pass so far, per Directive 4.

## Active Initiative (Stage 3 — Orchestrate, in progress; Stage 4 Verify not yet run)

**Backend platform migration + TypeScript conversion.** Trigger: AWS account closes 2026-08-16, current Lambda+DynamoDB backend will go dark and lose all data. Target: Express.js + PostgreSQL (Prisma) on the user's existing Hetzner VPS/Coolify, then strict-TypeScript conversion of the whole codebase. Full plan: `docs/PROJECT_PLAN.md`. REQ-1200 to REQ-1615 logged in `REQUIREMENTS.md`. Critical risk: `RISK_REGISTER.md` RISK-0005 (closed/accepted).

**Superseded — kept for history, not current status:** the paragraph below this section originally said "no code has been written yet" and "awaiting Gate 1 confirmation." That was true at the start of C1 but is now seven build passes out of date — see the "Fourth pass" through "Seventh pass" write-ups above for what's actually shipped. Phase 1 (backend migration + Google sign-in) is functionally complete and locally verified; Phase 3 (cache-invalidation audit) is complete; Phase 4 (UI/UX polish) is mostly complete; **Phase 2 (TypeScript/Vite) is now 100% complete** — backend and frontend both fully strict-typed, zero `.js`/`.jsx` remain anywhere in `src/`, `tsc -b`/`eslint`/`vite build` all clean, verified in a real browser across every page type. Only Coolify/VPS deployment (explicitly deferred by the user) remains not-started by design. `docs/PROJECT_PLAN.md` §6/§11 is the authoritative per-item checklist.

**All open decisions resolved 2026-07-30:**
- REQ-1303 → migrate CRA to Vite (Phase 2, not Phase 1).
- REQ-1402 → TanStack Query invalidation only, no SSE/WebSocket.
- Data strategy → REQ-1203 (DynamoDB export) is WONT-DO; REQ-1204 seeds fresh from `data/db.json` + new test accounts (`test@admin.com`, `test@user.com`, both `12345678`). RISK-0005 closed/accepted.
- New feature added to scope: Google one-click sign-in (REQ-1500..1505), OAuth2 redirect/callback pattern. Google Client Secret received in chat — RISK-0007 logged, secret only ever lives in gitignored `.env`/Coolify env vars, never in tracked files.

**Still blocking before Gate 2 (release sign-off):**
1. Independent (non-self) verification / Red Team review — everything built so far across all five passes was Build-Agent self-testing (Directive 4: self-testing ≠ independent verification).
2. Coolify/VPS deployment — explicitly deferred by user request, not a blocker for local development to continue.

## Ready For

**Idle — Infinity Loop active.** Await next user instruction:

1. **New feature / fix** → Specify (new `REQ-XXXX`) → Constrain → Gate 1 → Orchestrate (`build-agent-js`)
2. **Independent verify** → Stage 4 Red Team on shipped REQ-1200…1630 → Gate 2
3. **Deploy** → only if user explicitly asks (REQ-1201/1210–1212)

Shipped locally (Phases 1–6): Express+Prisma backend, Vite/TS frontend, Google OAuth, AI insights, address book + checkout wiring, page enrichment (products/orders/users/tickets/reviews), Sentry tunnel, cache/invalidation polish. Open optional: REQ-1400 (`queryKeys` module), REQ-1403 (Redis YAGNI), REQ-1615 (Vercel headers when deploy in scope).

## Ninth pass — multi-model AI Business Insights (REQ-1613), new session (2026-07-31)

A prior session hit its usage limit mid-message; this pass resumed with "continue what i said" plus a large combined ask (implement everything remaining, redesign pages with richer data, and "brainstorm with all those api key i added in docs/personal-dev-info.txt"). Re-ran `tsc -b`/`eslint` on both frontend and backend first — both already 0 errors (only the 7 known pre-existing accepted warnings), confirming no lint debt had accumulated. Scoped the ~30-key personal-dev-info.txt ask down to the one item it directly and concretely unblocks: REQ-1613 (multi-model AI insights), already named and planned in REQUIREMENTS.md/PROJECT_PLAN.md — see DECISION_LOG.md 21:00:00Z for the full reasoning on why the other ~20 keys (Clerk/Auth0, Contentful, ImageKit/UploadThing/Unsplash, ElevenLabs/Replicate/Fal/Jina, Sentry/PostHog/LangSmith/Langfuse) were deliberately NOT wired in this pass.

- Read the user's own `stock-inventory` reference project's real AI-insights implementation (`lib/ai/{groq,openrouter,create-chat-completion}.ts`, `app/api/ai/insights/route.ts`) rather than only following the two `LLM_MODEL_*.md` docs, per the user's explicit "like stock inventory... codebase" instruction.
- Built `backend/src/lib/ai/` — a generic OpenAI-compatible chat-completion client (`client.ts`) driven by a provider registry (`providers.ts`) covering 9 free-tier providers from `docs/personal-dev-info.txt`: Gemini, Groq, OpenRouter, Cerebras, NVIDIA NIM, Mistral, Cohere, GitHub Models, Hugging Face — tried in priority order, silently skipping unconfigured/failing ones (never depends on a single provider, per `LLM_MODEL_SELECTION.md`). One generic client instead of 9 near-duplicate provider files, since all 9 speak the same `/chat/completions` JSON shape.
- `backend/src/services/aiInsights.service.ts` + `POST /admin/ai-insights` (admin-only) take a client-computed analytics text summary (built by the new `buildAiInsightsSummary()` in `analyticsService.ts`, reusing data already in the TanStack Query cache — zero extra DB queries, matching this project's established "analytics computed client-side" pattern) and return 3-5 short recommendations. 10-minute in-memory response cache (SHA-256 of the summary, no Redis, per REQ-1403) so repeat dashboard visits within that window never re-trigger a provider call.
- Frontend: `useAiInsights` hook (`src/hooks/useAdmin.ts`, 10-minute `staleTime` mirroring the backend cache) + new "AI Business Insights" card on `AdminAnalyticsPage.tsx` (loading-pulse / error / not-configured states, manual Refresh button).
- Added the 9 real API keys to `backend/.env` (gitignored, local only) and documented empty placeholders in `backend/.env.example`.
- **Real bug caught during live verification, not just a clean build:** the first live call against real Gemini traffic truncated mid-sentence with `max_tokens: 400` — root-caused as Gemini 2.5 Flash (a reasoning-capable model) spending part of the token budget on hidden reasoning before the visible answer. Fixed by raising to `max_tokens: 1024`; re-verified with 4 complete, well-formed, data-grounded recommendations against the real seeded DB (reorder low stock, promote best-seller, re-engage new users, cross-sell). Also verified the 10-minute cache actually hits (`cached: true`) on an identical repeat request.
- **Verification:** `tsc -b`/`eslint` clean on both frontend and backend (0 errors). Real-browser Playwright pass: logged in as `test@admin.com`, navigated to `/admin/business-insights`, confirmed the AI Business Insights card renders 4 real insights with provider/model attribution, the Refresh button works, 0 console errors.
- `.agile-v/REQUIREMENTS.md` REQ-1613 updated DONE; `docs/PROJECT_PLAN.md` §6 item 13 and the "Not started/deferred" list updated; `DECISION_LOG.md` gained 5 new entries covering the scope decision, the provider-generalization choice, the client-computed-summary choice, the no-Redis cache choice, and the max_tokens bug.

**Still open / explicitly not done this pass:**
- Sentry/PostHog observability (keys present in `docs/personal-dev-info.txt`, dedicated `docs/Redis_Sentry_PostHog_INTEGRATION_GUIDE.md` exists) — not wired; a legitimate future feature but out of this pass's scope (not named in REQUIREMENTS.md, no explicit user ask this round beyond "as much as you can" from the keys file).
- RippleButton wider rollout (REQ-1607) and deeper nested KPI-card conversion (REQ-1608) — unchanged, still partial.
- Coolify/VPS deployment — still explicitly off-limits per standing user instruction.
- Independent (non-self) verification / Red Team review — still outstanding across every pass so far, per Directive 4.

**Update (same session, continued): REQ-1616 — Product catalog data-enrichment, phase 1 of the page-redesign ask.** User's answer to the scoping question ("everything, all pages," "add genuinely new data fields") confirmed a large multi-domain initiative; started with Product (explicitly named a priority). Added 8 new `Product` fields (`sku`/`isbn`/`publisher`/`publishedYear`/`language`/`edition`/`fileFormat`/`tags`) — deliberately digital-catalog-appropriate only, not physical-inventory fields (weight/dimensions/warehouse), after confirming via the storefront's own FAQ that every title is a pure digital download, never physically shipped (adding physical metadata would have been internally inconsistent fake data). `prisma db push --accept-data-loss` (safe — `sku` nullable, no dupes). All 15 seed products hand-enriched with real, grounded values and reseeded. New "Book Details" section on `ProductDetail.tsx`, "Catalog & Inventory Metadata" card on `AdminProductDetailPage.tsx`, matching `ProductForm.tsx` inputs (create+edit). **Real bug caught live:** `product.tags.length` crashed (`TypeError`) on first load — the persisted TanStack Query cache (REQ-1310) held a pre-change product shape without `tags`; fixed by relaxing the frontend type to properly nullable/optional with defensive reads everywhere (durable fix for any future schema drift) AND bumping `package.json`'s version to trigger the existing `__APP_VERSION__` cache-buster (REQ-1310's own invalidation mechanism, just not triggered until now) — verified both fixes together live (form fields empty pre-bump, correctly populated post-bump). Verified end-to-end: `tsc -b`/`eslint` clean both sides, live browser edit→save→revisit round-trip against the real DB (test edit reverted after confirming persistence). Remaining domains (Orders, Users, Tickets, Reviews, Dashboard) not started — flagged in `docs/PROJECT_PLAN.md` §11 as the next candidates, same one-domain-at-a-time approach.

## Tenth pass — LLM docs merge + Order Detail (REQ-1617) + Address Book/Order History (REQ-1618), continued session (2026-08-01)

Continuing the same session past the Ninth pass, the user's next message combined two asks in one: (1) merge `docs/LLM_MODEL_SELECTION.md` + `docs/LLM_MODEL_IMPLEMENTATION_GUIDE.md` into one generic, portable reference doc with live web-verified free-tier provider status, and (2) "continue wit remain from Order detail (shipment timeline, itemized breakdown), User detail (address book, order history), order history as user with all clickable etc."

- **Docs merge:** dispatched a background research agent (isolated worktree, WebSearch-enabled) to do the merge in parallel with app-code work, since it's a large, independent, non-code task. Verified the agent's output before trusting it — read the full 297-line merged doc, confirmed the old file was deleted and cross-references updated. The agent's own research surfaced a real bug: `backend/src/lib/ai/providers.ts`'s Groq model chain still had `qwen/qwen3-32b`, already deprecated/shut down 2026-07-17 per Groq's own deprecation page. Fixed to `qwen/qwen3.6-27b`.
- **REQ-1617 — Order Detail:** new `GET /orders/:id` (ownership-checked, embeds a chronological timeline derived from existing `ActivityLog` rows — zero schema change) and a new `/orders/:id` customer page (itemized cartList breakdown, timeline, reused `OrderTrackingInfo`). Dashboard order cards now link to it. Caught and fixed a real naming collision (`getOrderById`/`useOrder` would have collided with the pre-existing admin versions in the shared barrel) — renamed to `getOrderDetail`/`useOrderDetail`.
- **REQ-1618 — Address Book + Order History on User Detail:** investigated first and found `AdminUserDetailPage.tsx`'s doc-comment claimed it already showed orders — it didn't; also found a real justification for an address book: `shipping.service.ts`'s Shippo label generation already reads `order.shippingAddress`/`order.address` but nothing ever writes either (checkout has no address collection, confirmed via `StripeCheckout.tsx`'s own on-screen note). Added a new `Address` model + self-service `/addresses` CRUD (ownership always from the JWT), single-default-per-user enforced in the service layer. New `AddressBook.tsx` on the customer Dashboard (inline add/edit, `AlertDialog` delete confirm). `GET /admin/users/:id` now embeds `orders` + `addresses`; new read-only "Address Book" + "Order History" cards on `AdminUserDetailPage.tsx`, order rows linking to `/admin/orders/:id`. Deliberately left checkout unwired to addresses (separate, higher-risk payment-flow change, not named in the ask).
- **Order history "all clickable" audit:** confirmed already satisfied — Dashboard order-ID badge links to `/orders/:id` (REQ-1617), product links pre-existing, `OrderTrackingInfo`'s tracking-number/label links pre-existing. No further gaps found.
- **Verification:** `tsc -b`/`eslint` clean on both frontend and backend (0 errors, only the same 7 pre-existing warnings). Full live-browser pass as both roles: created/edited/deleted an address as the demo customer (instant UI update each time, correct toasts, first address auto-defaulted), confirmed it and the customer's order rendered on the admin's new cards, clicked through to the real admin order detail page, deleted the test address afterward so no leftover test data remains in the seeded dev DB.
- `.agile-v/REQUIREMENTS.md` gained REQ-1618 (DONE); `docs/PROJECT_PLAN.md` §6 item 18 added, §11 and the traceability line updated; `DECISION_LOG.md` gained 11 new entries across the docs-merge, REQ-1617, and REQ-1618 work.

**Still open / explicitly not done this pass:**
- Tickets and Reviews domains not yet given a data-enrichment pass (next candidates for the page-redesign initiative).
- Checkout not wired to the new address book (flagged as a natural follow-up now that real address data can exist).
- RippleButton wider rollout (REQ-1607) and deeper nested KPI-card conversion (REQ-1608) — unchanged, still partial.
- Coolify/VPS deployment — still explicitly off-limits per standing user instruction.
- Independent (non-self) verification / Red Team review — still outstanding across every pass so far, per Directive 4.

## Eleventh pass — Tickets + Reviews data-enrichment (REQ-1619), continued session (2026-08-01)

User replied "yes continue" to close out the page-redesign initiative's final two named domains: Tickets and Reviews.

- **Tickets**: added `priority` (low/medium/high/urgent, default medium), `category` (billing/technical/refund/account/other, default other), and an optional `orderId` link — all genuinely useful triage fields, with `orderId` directly grounded in the storefront's own FAQ ("open a support ticket... for refunds"). `orderId` is ownership-checked server-side (a ticket may only reference an order that actually belongs to the reporting user, mirroring the existing check in `reviews.routes.ts`). New `PUT /tickets/:ticketId/priority` (admin-only). Wired through `CreateTicketPage.tsx` (category/priority selects, an optional order dropdown sourced from the user's real orders), `TicketDetailPage.tsx` (priority badge for customers, editable dropdown for admins, order link), `TicketsListPage.tsx` (priority badge shown only when non-default), and `AdminTicketsPage.tsx` (new Priority column + filter).
- **Reviews**: added `adminReply`/`adminReplyAt` — a public "store response," the standard seller-reply-to-review pattern. Also added a "Verified Purchase" badge with **zero schema change**, since every review already requires a real `orderId` at creation (enforced in `reviews.routes.ts`) — this was simply always true and just needed a UI badge. Gave Reviews its first-ever admin detail page (`AdminReviewDetailPage.tsx` at `/admin/reviews/:id`) rather than trying to shoehorn an inline reply form into the shared `DataTable` component (no row-expansion API, used by 6+ other admin pages) — this also closes a real structural gap, since Products/Orders/Users/Tickets all already had list+detail while Reviews only had a list. `AdminReviewsPage.tsx` row-click now navigates there; existing Link/status cells got `stopPropagation` to avoid double-navigation.
- **Real bug caught and fixed proactively**: re-examining `AdminUserDetailPage.tsx` (shipped earlier this same session as REQ-1618) found it read `user.addresses.length`/`user.orders.length` with no fallback — the identical crash class as REQ-1616's `product.tags` bug (a persisted query-cache entry from before that field existed). Fixed with local `addresses`/`orders` consts defaulting to `[]`, and relaxed `AdminUserDetail.orders`/`addresses` to optional in the type.
- `package.json` bumped 0.2.2 → 0.2.3 (one cache-buster covering both the new required `Ticket` fields and the `AdminUserDetail` fix). Both dev servers restarted in their own directories after a first attempt accidentally launched two frontend instances (caught via `ps aux`, corrected immediately).
- **Verification:** `tsc -b`/`eslint` clean on both frontend and backend (0 errors, same 7 pre-existing warnings). Full live-browser pass: created a real ticket with category "Refund Request," priority "High," and a linked real order as the demo customer; changed its priority to "Urgent" as the demo admin (instant toast + UI update); posted a store reply to a seeded review from the new `AdminReviewDetailPage.tsx` and confirmed it rendered correctly on the live customer-facing product page next to the Verified Purchase badge; 0 console errors throughout.
- `.agile-v/REQUIREMENTS.md` gained REQ-1619 (DONE); `docs/PROJECT_PLAN.md` §6 item 19 added, §11 updated to reflect the page-redesign initiative as fully complete across every domain in the user's original ask, traceability line bumped to REQ-1619; `DECISION_LOG.md` gained 6 new entries.

**Still open / explicitly not done this pass:**
- Checkout not wired to the new address book (deliberately deferred in REQ-1618, unchanged).
- RippleButton wider rollout (REQ-1607) and deeper nested KPI-card conversion (REQ-1608) — unchanged, still partial.
- Coolify/VPS deployment — still explicitly off-limits per standing user instruction.
- Independent (non-self) verification / Red Team review — still outstanding across every pass so far, per Directive 4.

## Twelfth pass — layout fix, CRA cleanup, Sentry tunnel, post-implementation deep audit (REQ-1626–1630), continued session (2026-08-01)

- REQ-1626: fixed page-width misalignment (Login/Register visibly narrower than Header/Footer). Root cause was two-fold: `AuthSplitLayout`'s own nested `max-w-6xl` inside the shared `max-w-9xl` wrapper, and a legacy global `main { max-width: 1280px; ... }` CSS rule in `index.css` silently overriding the Tailwind width system on 12 pages — removed, replaced its sticky-footer behavior with a standard `flex min-h-screen flex-col` / `flex-1` pattern. Verified via Playwright `boundingBox()` pixel measurements, not just visual comparison.
- REQ-1627: CRA-cleanup audit — no CRA scaffold files remained from earlier rounds; removed the dead `web-vitals` dependency and one stale doc comment.
- REQ-1628: Sentry error monitoring implemented Sentry-only (Redis/PostHog from the same integration guide deliberately not implemented, out of scope) — `@sentry/react` + `@sentry/node`, translated from a Next.js-specific guide since this is Vite+Express. `POST /api/monitoring` tunnel endpoint (parses the DSN from the SDK's own envelope, forwards server-side) so ad-blockers can't drop error reports in either normal or incognito browsers. Verified against the real user-provided Sentry project: curl envelope round-trip, live browser capture with zero direct `sentry.io` requests, real source-map upload during build.
- REQ-1629: found and recovered 3 silently-vanished `public/` files (`manifest.json`, `logo192.png`, `logo512.png`) — a real console error — from git's staged index (never committed, so index was the only recovery source).
- REQ-1630: post-implementation deep audit. Found and fixed 2 real gaps: dead `@testing-library/*` devDependencies (CRA-scaffold leftover, 0 test files, missed by REQ-1627's config-only cleanup) uninstalled; both `.env.example` files were missing several genuinely-used env vars — documented. Re-confirmed query-invalidation coverage, Sentry tunnel, and layout fix all consistent; re-confirmed the large staged `codebook/**` deletions are the pre-existing, already-approved REQ-1101/1102/1308 restructure, not new data loss.
- `tsc -b`/`eslint`/`vite build` clean on both frontend and backend throughout (0 errors, same 7 pre-existing accepted warnings). Everything staged via `git add -A`, not committed until this pass's end (see DECISION_LOG for the commit entry once pushed).
- New this pass: `CLAUDE.md` created at repo root — compact per-session memory/orientation file for future agent sessions (stack, conventions, current status, key gotchas), kept intentionally short and separate from this file's full history.

## Thirteenth pass — university-library catalog enrichment: book-cover art, trailer video, recommendations reel, admin insights (REQ-1634–1635), continued session (2026-08-01)

- REQ-1634: added nullable `coverColor`/`videoUrl` to `Product` (`prisma db push` — project has no migrations folder, uses db push exclusively). New shared components `BookCover` (memoized SVG 3D-book cover, ported from the user's separate `university-library` project, falls back to flat image when unset), `ProductVideo` (native `<video>`, renders nothing without a URL), and a first version of `ProductReel`. New `useRecommendedProducts` hook derived from the already-cached products list (no extra fetch). Wired into `ProductCard`, `ProductDetail`, admin CRUD (form + detail + table thumbnail). `adminService.getAdminStats()` extended client-side (category/language/year/top-rated/health breakdowns) with a new `AdminCatalogInsights` dashboard section — no backend change needed, already had the full products list cached.
- REQ-1635: user feedback follow-up. Merged all 17 books from the reference project's `dummybooks.json` as new products (15→32 catalog total), downloading their cover images into `public/images/` instead of hotlinking. Fixed 6 too-light `coverColor` instances (literal `#ffffff` ×2 + pale `#fff9bf` ×1, each recurring once in the original 15 and once in the merged 17) via a perceived-luminance check — user had spotted the washed-out white covers directly in their own screenshot. Rewrote `ProductReel` from a `setInterval`+`scrollBy` discrete-jump design to a single `requestAnimationFrame` loop driving `transform` on a track duplicated once for a seamless infinite ticker-tape loop; pause-on-hover, resume, and manual chevron nudges all share the same offset so they never conflict. Added shelf-gradient background + drop-shadow + hover-lift to the book-cover presentation on the card, detail page, and admin detail page.
- Verified live in-browser throughout: cover art on list/detail/admin, video playback, reel smoothness (sampled transform values confirm continuous ~32px/s drift, not jumps), pause/resume/manual-nudge, and a real admin edit propagating instantly to the admin table + storefront list + detail page via client-side navigation with zero page reloads (TanStack Query invalidation).
- `tsc -b`/`eslint`/`vite build` clean both sides throughout; `npm audit`/`depcheck` re-confirmed no new issues (both pre-existing accepted risks unchanged). `package.json` bumped to 0.2.4 (AdminStats/Product query-shape change).
- REQ-1636: second follow-up. Removed the 15 original CodeBook products entirely — kept only the 17 merged library books (checked first that `Review.productId`/order `cartList` have no FK constraint, deleted 1 orphaned review + the 15 product rows + their now-unused images safely). Caught a real gap this introduced: zero products were `featured_product`, which would've left the homepage's Featured section empty — marked 3. Admin `ProductForm`'s Cover & Media section gained a live `BookCover` preview (previously raw inputs, no visual feedback). `ProductDetail` completely redesigned — replaced the old wall of 6+ colored info pills with a clean hierarchy (eyebrow/title/author/rating, one-line stock status, prominent price+CTA, a scannable Book Details grid, QR code demoted to a collapsible utility, Description in its own section) — logic untouched, layout only.
- Verified live: catalog is exactly 17 products via API, admin cover preview updates in real time on color change, redesigned ProductDetail screenshotted end-to-end (hero, description, recommendations reel). `tsc -b`/`eslint`/`vite build` clean both sides.

## Fourteenth pass — payment-tampering security fix (REQ-1637), continued session (2026-08-01)

- Routine audit (checking mutation-invalidation coverage, not a new feature ask) surfaced a real vulnerability: `POST /payment/create-intent` trusted a client-supplied `amount` for the actual Stripe charge, and both that route and `POST /orders` let request-body/metadata fields override the JWT-verified user identity (old code spread client `metadata` *after* the trusted `userId`/`userEmail`/`userName` fields).
- Fixed: `payment.routes.ts` now takes only `cartList: {id, quantity}[]`, bulk-looks-up live prices via new `products.service.getProductsByIds`, and recomputes the charge total itself (rejects if a product is gone or total < $0.50); no longer accepts any client `metadata` at all — identity fields come solely from `req.user`. `orders.routes.ts`'s `POST /orders` now retrieves the real PaymentIntent from Stripe whenever `paymentIntentId` is present, checks it belongs to `req.user` and is `succeeded`, and overwrites `amount_paid` with the verified charged amount — so a stored order (and admin revenue analytics) can never diverge from what Stripe actually charged.
- Frontend simplified to match: `paymentService.createPaymentIntent(cartList)` dropped the now-redundant `amount`/`user` params; `usePayment.ts`/`StripeCheckout.tsx` updated.
- Verified against a real Stripe test-mode charge end-to-end (not just code review): added a $9 product to cart, confirmed `create-intent` recomputed exactly $9.00 server-side, paid with Stripe's `4242...` test card, confirmed the DB order's `amount_paid` (9) exactly matched the independently-retrieved PaymentIntent's charged amount, confirmed the cart badge updated 1→0 instantly with no page refresh — then deleted the test order and restored the decremented stock so no synthetic data was left in the demo DB.
- `tsc -b`/`eslint --max-warnings=0`/`vite build` clean both sides.
- Declined as out of scope this pass (flagged to user, not silently ignored): (1) the user's boilerplate SSR/`page.tsx`/"use client" instructions don't apply — this project is a Vite SPA with React Router 7, no SSR by design (see CLAUDE.md); (2) "add as much AI automation as possible" is unbounded scope creep with no concrete spec — needs a specific ask before implementing, per the project's own no-unrequested-features convention.

## Fifteenth pass — full 18-item punch list from the prior audit (REQ-1638–1654), continued session (2026-08-01)

- Wrote the full item list into `docs/PROJECT_PLAN.md` Phase 8 first (per user request), then implemented one by one following `docs/PROJECT_ENGINEERING_PLAYBOOK.md` §2/§6.3 (confirmed no SSR/dehydrate path applies to this Vite SPA — recurring user instruction about `page.tsx`/`use client` split doesn't apply here, per the playbook's own stack rule).
- **UI consistency:** REQ-1638 (equal-height KPI/product cards + description clamp), REQ-1647 (order-status KPI breakdown badges — found the Analytics page missing what the Dashboard already had).
- **Commerce-critical fixes:** REQ-1639+1643 (auto-refund on cancel + reason capture, same code path — also fixed a real latent bug: the "Refunded" status-dropdown option was never backend-valid), REQ-1640 (on-demand invoice download), REQ-1641 (idempotency via a unique `paymentIntentId` constraint + pre-check + race-condition safety net), REQ-1642 (rate limiting on payment/order routes).
- **Per-entity analytics:** REQ-1644 (product detail — sales trend sparkline + refund/cancel rate), REQ-1645 (user detail — lifetime value, order breakdown, last-order date, refund history), REQ-1646 (order timeline — found the admin page had *none* at all, only the customer page did; extracted a shared `OrderTimeline` component).
- **"AI-driven" items (REQ-1648, 1650, 1652, 1653 implemented as deterministic math, not LLM calls — disclosed as a judgment call, more reliable/instant/free than a model guess at arithmetic):** restock forecast (days-until-stockout from order velocity), order risk flag (3×+ a customer's own average), suggested pricing (sell-through rate, admin-approve-only), churn risk (days-since-last-order vs. that customer's own average interval). REQ-1649 (sales narrative) needed no new code — already covered by the existing AI Insights panel. REQ-1651 (review sentiment) is the one genuine LLM feature, run on-demand per admin click to keep cost/latency opt-in.
- REQ-1654 (low-stock digest email) — real, working, admin-triggered; deliberately no cron/scheduler added (new infra beyond this pass's scope, noted as a scope decision, not silently skipped).
- Every item verified against real external state: a real Stripe test-mode refund (paid→cancelled→refunded, stock restored to exact original), a real duplicate-request idempotency test (2 identical requests, 1 DB row, stock decremented once), a downloaded and `file`-validated PDF, a real Brevo digest send, real multi-provider LLM output (Gemini) for review sentiment.
- `tsc -b`/`eslint --max-warnings=0`/`vite build` clean on both sides after every single item, plus a final whole-codebase (not just touched-files) sweep at the end. No new npm dependencies added anywhere in this pass.

## Sixteenth pass — post-implementation deep audit of REQ-1638–1654 (REQ-1655), continued session (2026-08-02)

- User asked for a full re-audit before testing/committing: gaps, misconfig, invalidation coverage, auth/zod, dead code, lint/build — not new features unless a real gap surfaced.
- Re-verified (not just re-read) every REQ-1638–1654 change: `tsc -b`/`tsc --noEmit` + `eslint --max-warnings=0` + `vite build`/backend `build` clean both sides; every new/changed mutation hook (`useAdmin.ts`, `useReviews.ts`) invalidates the right Query keys or is intentionally invalidation-free with an inline comment explaining why (ephemeral sentiment/digest results); every new/changed route has `requireAuth`/`requireAdmin` + zod (`createOrderSchema`) or manual narrowing on body fields, plus ownership checks; no `any`, no dead code, no leftover debug logs, no duplicate fetches (`ProductForm`'s suggested-price feature shares the `["admin-orders"]` cache key with `AdminOrdersPage`, confirmed via `useAllOrders`); confirmed the pre-extraction inline order-timeline code was fully removed from `OrderDetailPage.tsx`, not duplicated.
- One real gap re-surfaced (previously flagged in REQ-1654 but left unfixed): `POST /email/send` had no restriction on `to` — any authenticated non-admin user could email any template to any address (open relay). User chose to fix now rather than defer. Fixed by requiring non-admin callers' `to` to equal their own `req.user.email` or the fixed `ADMIN_ALERT_EMAIL`; admins remain exempt. Confirmed this doesn't break any legitimate caller (`emailService.ts`'s `sendEmail` is only ever called with the customer's own email or the fixed admin address today).
- No other gaps found. `tsc`/`eslint`/build clean after the fix.

## Seventeenth pass — Phase 9: 12 new commerce/admin/AI features (REQ-1656–1667), continued session (2026-08-02)

- User scoped 8 of a 10-item feature list (declined saved payment methods and 2FA, both accepted as-is), then said "all, in order" to implement. Mid-round, caught and disclosed that "Refresh tokens" had been accidentally dropped from the initial Phase 9 write-up despite being in the user's own quoted list — added back as REQ-1667 and implemented in its original sequence position.
- Schema: added `Wishlist`, `StockAlert`, `Coupon`, `ReturnRequest`, `RefreshToken` models plus `Order.isGuest`/`guestEmail`/`couponCode`/`discountAmount` in one `prisma db push` pass; enabled the `pg_trgm` Postgres extension on the dev DB for REQ-1665.
- Implemented and verified each item individually (`tsc`/`eslint --max-warnings=0` on both sides after every single one, not batched): REQ-1656 wishlist, REQ-1657 back-in-stock alerts (fires on stock restoration via *either* admin edit or order-cancel path), REQ-1658 coupons (server-side-only discount computation, same discipline as the REQ-1637 payment-tampering fix), REQ-1659 guest checkout (synthetic `guest_<uuid>` identity so every existing ownership check keeps working unmodified), REQ-1660 Shippo tracking webhook (secret-in-URL-path verification, since Shippo doesn't sign webhooks like Stripe), REQ-1661 node-cron scheduler (opt-in via `SCHEDULE_JOBS_ENABLED`, default off), REQ-1662 CSV import/export (products only for import — orders are transactional records, export-only by design), REQ-1663 return/RMA flow (refactored `refundOrderPayment()` out of `orders.routes.ts` into `orders.service.ts` so it could be reused, not reimplemented a third time), REQ-1667 refresh tokens (access token 7d→1h, rotating refresh token, revocation on logout), REQ-1664 AI product-description generator, REQ-1665 fuzzy search, REQ-1666 AI ticket-reply drafts.
- Real bugs found and fixed via live testing, not code review alone: (1) `/payment-success`/`/payment-cancel` were wrapped in `<ProtectedRoute>`, which would have redirected any guest checkout straight to `/login` post-payment, losing the order entirely — unwrapped both (REQ-1659). (2) The AI product-description generator's `long_description` truncated mid-sentence at `max_tokens: 512` — same reasoning-token-budget root cause as the earlier REQ-1613 AI Insights bug; raised to 1024 and re-verified (REQ-1664).
- Live-verified against real external state, not just compiled: full refresh-token rotation chain against the real dev DB (login → rotate → old-token-rejected → new-token-authorizes-a-route → logout → revoked-token-rejected); the exact "atomic habbits" typo case named when REQ-1665 was scoped now correctly returns "Atomic Habits" via `pg_trgm`; real Gemini output for both the product-description generator and the ticket-reply-draft feature (created and then deleted a real test ticket); confirmed `SCHEDULE_JOBS_ENABLED` defaults off via a live server-boot log.
- New npm dependencies, each justified by a genuinely new capability: `node-cron`/`@types/node-cron` (first background-job infra in this backend), `csv-parse`/`csv-stringify` (RFC4180-correct CSV handling — a hand-rolled parser risks silent corruption on a description field containing a comma).
- Final whole-codebase sweep (not just touched files): `tsc -b`/`eslint --max-warnings=0`/`vite build` (frontend) and `tsc --noEmit`/`eslint --max-warnings=0`/build (backend) all clean; `git status` confirmed only the expected ~75 files changed, no stray artifacts; all synthetic test data (a test ticket) deleted from the dev DB afterward.

## Eighteenth pass — post-implementation deep audit of REQ-1656–1667 before commit (2026-08-02)

- User asked, pre-commit, for a full re-audit of all Phase 9 work: gaps, misconfig, invalidation coverage, auth/zod, secrets handling, scalability, dead code, lint/build — explicitly no new features, only fixes for real gaps.
- Re-verified: `tsc`/`eslint --max-warnings=0` + production `build` clean on both sides (including a Sentry sourcemap upload on the FE build and a Prisma `db push` confirming zero schema drift); every one of the 26 new/changed files from REQ-1656–1667 confirmed wired (registered in `app.ts`, routed in `AllRoutes.tsx`, exported from `pages/index.ts`, referenced ≥1 place) — no orphaned files; every new hook's Query keys and `invalidateQueries` calls traced end to end (`useReturns`' approve-return invalidates `admin-returns`, `admin-order`, `admin-orders`, `order`, **and** `admin-products` for the stock-restore side effect); zod schemas confirmed present on every new route that takes a body.
- Three real minor gaps found and fixed: (1) Shippo webhook secret was compared with `!==` (timing side-channel) — switched to `crypto.timingSafeEqual` via a new `isValidWebhookSecret()` helper. (2) `POST /stock-alerts` (public, unauthenticated, email-collecting) had no rate limiter — added a new `publicWriteLimiter` (20/15min) to `rateLimit.ts` and applied it. (3) `src/services/index.ts` barrel was missing 5 of the new services (`couponService`, `returnService`, `stockAlertService`, `guestOrderService`, and `ticketService`'s two newest exports) — every existing service was fully barrel-exported except these; completed it for consistency, even though internal hooks import the concrete files directly.
- No functional, architectural, or security defects found beyond those three — refresh-token rotation, guest-checkout ownership checks, coupon server-side-only discount math, CSV RFC4180 handling, and the AI reasoning-token-budget fix (REQ-1664) all held up under this second pass.
- `tsc`/`eslint`/build re-verified clean after the three fixes, both sides.

## Nineteenth pass — second, broader pre-commit audit after REQ-1668 (REQ-1669), continued session (2026-08-02)

- After REQ-1668 (Phase-9-diff-scoped audit, 3 gaps found/fixed) was committed and pushed, user asked to audit again, this time explicitly whole-codebase rather than diff-scoped, since the prior round had found real issues.
- Widened scope beyond the 26 Phase-9 files to everything: all 18 frontend hooks (not just the 6 new ones) checked for mutation/invalidation correctness; all 18 backend route files (not just the 5 new ones) checked for auth-guard/zod coverage; a heuristic orphaned-file reference-count scan across every file in `src/` and `backend/src/`; `depcheck`/`npm audit` both sides; a full `process.env`/`import.meta.env.VITE_*` vs `.env.example` cross-check; a repo-wide `location.reload()`/CRA-`process.env`-leak sweep.
- Result: zero new gaps found. The 3 fixes from REQ-1668 were the complete set of real issues — this pass confirms it rather than finding more. A few grep-based false positives were investigated and ruled out (hook mutation counts skewed by counting the `useMutation` import statement itself; `express.d.ts` "orphan" is a global ambient type augmentation, correctly zero-imported by design; `depcheck` flagging `tailwindcss`/`postcss`/`autoprefixer` as unused, confirmed still wired via `.cjs` config files it doesn't parse; frontend's one high `npm audit` advisory is the pre-known RSC-mode-only react-router CVE, inapplicable to this client SPA).
- `tsc`/`eslint --max-warnings=0`/build clean both sides (re-verified, no changes needed). No commit made this pass — nothing changed in the codebase, only verification.

## File Index

| File | Purpose |
|---|---|
| `REQUIREMENTS.md` | Single source of truth — all REQ-IDs |
| `DECISION_LOG.md` | Append-only rationale trail |
| `VALIDATION_SUMMARY.md` | Evidence summaries per stage |
| `PLAYBOOK.md` | How this project runs the Agile-V loop day to day |
| `ATM.md` | Requirement → code/test → verification traceability matrix |
| `CHANGE_LOG.md` | Change requests across cycles |
| `RISK_REGISTER.md` | Known risks + mitigations |
| `CAPA_LOG.md` | Corrective/Preventive actions from failures |
| `APPROVALS.md` | Human Gate approval records |
| `REVALIDATION_LOG.md` | Periodic re-verification of BASELINE REQs |
| `POLICY.yaml` | Policy-as-code (risk tiers, gate rules) |
| `TRACE_LOG.md` | Append-only tool/policy execution spans |
| `EVAL_RESULTS.md` | Eval flywheel + Gate 2 eval_gate_status |
| `CHECKPOINTS.md` | Durable Human Gate interrupts (PENDING/RESOLVED) |
| `config.json` | Machine-readable project/stack config |
| `phases/` | Per-phase PLAN/SUMMARY/CONTEXT (created as stages run) |
| `cycles/C1/` | Frozen archive, populated when C1 closes |
